# 《「言」之有物：从数据看中国电影的答案》全项目 Review 报告

- 日期：2026-08-30（第八届中国数据内容年度案例征集提交截止日）
- 评审范围：数据口径与冻结状态（v4.7）、脚本链路、测试、前端契约与构建、文档一致性、版本管理与发布就绪度
- 评审基线：分支 `cursor/archive-volume-pages`（f860fc3，领先 origin/main 2 个提交，未推送）+ 工作区未提交改动（81 个文件）
- 评审方法：独立复跑全部验证（verify / unittest / npm run check / npm run test:js / HTTP 抽查），逐层比对指纹与计数

---

## 一、总体结论

| 层面 | 状态 | 结论 |
| --- | --- | --- |
| 数据层（v4.7 口径、指纹、冻结） | 🟢 通过 | `verify_freeze_readiness.py` 20/20 全 PASS；指纹 `70addfb0…` 六件套（manifest / narrative / aggregated / dialect_aggregates / geo_enrichment / story_universe）全部同步 |
| 测试层 | 🟢 通过 | 66 项 Python 测试全过；JS 单测 8/8 全过 |
| 前端构建与契约 | 🟢 通过 | `npm run check` 全绿：check:js ✓ / check:budget（238.1KB < 300KB）✓ / check:contracts ✓（flopCaseIds 4 个唯一值，08-27 的 6068516 重复 ID 问题已修复）；本地 HTTP 抽查关键资源 200 |
| 文档一致性 | 🟡 基本同步、5 处残留 | README 核心发现表已 v4.7，但仍有 5 处 v4.6/旧值残留；定义文档已 v4.7 |
| 版本管理 / 发布流程 | 🔴 高风险 | 81 个文件未提交 + 2 个提交未推送；**HEAD 处于"数据 v4.6 + 测试/文档 v4.7"的不一致状态，当前 HEAD 在 CI 上必红**；今日即截止日 |
| 评审期间并发活动 | ⚠️ 已记录 | 评审过程中（19:14–19:17）检测到外部进程并发修改工作区（tests、README、定义文档、report_data_strict.json 被更新至 v4.7），首次测试运行 5 失败、随后全过，根因即并发更新而非代码缺陷 |

一句话结论：**数据、测试、契约三层在当前工作区状态（v4.7 全量未提交改动）下已全部通过，可以提交；但提交动作必须整包进行（数据 + 测试 + 文档一起），绝不能只提交部分文件，否则 HEAD/CI 会因 v4.6 数据与 v4.7 断言错配而红。剩余工程动作集中在：README 残留清理 → 整包提交 → 合并 main → 推送 → 部署验证。**

---

## 二、数据层：口径在 08-27 后又演进两次（v4.6 → v4.7），当前冻结完好

### 2.1 口径演进（08-27 Review 之后）

| 版本 | 时间 | 变更 | 方言/普通话（China） | 指纹 |
| --- | --- | --- | --- | --- |
| v4.5 | 08-27（上次 Review） | Region=第一制片国 | 3,045 / 9,746 | `ecd258a8…` |
| v4.6 | 08-30 | 豆瓣「语言」全量回填 delivery 缺列 + Wikidata P364 补空缺 + 《隐入尘烟》人工补收 | 3,067 / 9,724 | `883fe6f7…` |
| **v4.7（当前）** | 08-30 | 豆瓣**部分抓取**终态：未抓到的 China 空语言默认普通话并戳 `EMPTY_LANG_DEFAULTED`；跳过 118 部 Wikidata 铸造的 China 方言 | **3,076 / 9,715** | **`70addfb0…`** |

China 总数恒为 12,791；全量方言 3,360 → 3,369。

### 2.2 v4.6 → v4.7 的实际影响（9 部增量全部在 2020s）

- 2020s 中国方言片：**79 → 88 部**；2020s 均分差 delta：+0.57 → **+0.52**
- 方言总均分：6.62 → 6.61；mean_delta：0.5037 → **0.5012**
- Tier：Tier1 2,303 → **2,309** / Tier2a 418 → **421** / Tier2b 346（不变）
- 双栖导演：478 → **479** 位；方言更高占比 70% → **69%**；平均分差 +0.65 → **+0.64**
- 粤语 2,769 → 2,776 / 非粤语方言 485 → 487（story_universe 同步）
- **故事结论方向全部不变**：方言 vs 普通话 +0.50 分、烂片率 6.5% vs 24.5%（3.8×）、1990s −0.39 / 2010s +0.95 反超

### 2.3 冻结验证（本次实测）

- `verify_freeze_readiness.py` **20/20 PASS**：B1 三方计数一致（3,076/9,715）、B2 Tier 基线（2309/421/346）、B3 方案A 56 + 审计 22 无残留 + Language_Code 不变量、B4 五件套指纹同步（`70addfb0…`）、B5 故事结论方向。
- 64 个详情分片指纹同步更新（每个 +1 行指纹字段）。
- manifest 溯源链完整：`language_backfill_20260830`（overrides 2,504 → applied 2,385 / dialect 30 / tier2b_excluded 19 / skipped_wikidata_china_dialect 118）与 `yinruchenyan_lang_fix_20260830`（LANG_FIX_20260830）。
- replay 链（`replay_v44_baseline.py`）已含 9 个补丁：tier2b → 空语言回填 → **豆瓣语言回填(v4.6/v4.7)** → 审计排除 → F7 → 戏曲排除 → 阿嬷 → **隐入尘烟** → 第一制片国。`--full-rebuild` 可一键回基线。

---

## 三、测试层：66 项全过（当前工作区）；HEAD 状态不一致需注意

- **工作区（v4.7 数据 + v4.7 断言）**：`python -m unittest discover -s tests` → **Ran 66 tests, OK**（含 DialectAggregatesTests 对 2020s=88、flop 14.8/32.9、双栖 479 等 v4.7 值的锁定）。
- **JS 单测**：`npm run test:js` → 8/8 通过。
- ⚠️ **HEAD（已提交 f860fc3）状态是"数据 v4.6 + 断言 v4.7"**：由于 v4.7 数据文件（81 个）尚未提交，而 tests/README 已含 v4.7 断言，当前 HEAD 上跑测试会 5 项失败（flop_decade 2020s、方言均分、非粤语方言 n、双栖导演 total、README 指纹）。**这是"必须整包提交"的直接原因**——CI（push main / PR）在未提交 v4.7 数据的任何中间状态都会红。
- 评审期间首次运行曾现 5 失败，随后复跑全过：原因是 19:16–19:17 外部进程把测试期望同步到 v4.7（tests/test_pipeline.py mtime 19:13 → 19:16）。已用 git diff 与 mtime 双重确认，非代码缺陷、非评审操作所致。

---

## 四、前端构建与契约：全绿

| 检查 | 结果 |
| --- | --- |
| `check:js` | ✅ 通过 |
| `check:budget` | ✅ 主包 gzip 238.1KB（预算 300KB），lazy chunk 9.6KB（预算 200KB） |
| `check:contracts` | ✅ flopCaseIds `[26796665, 22557335, 6068516, 3874981]` 4 个唯一值——08-27 Review 指出的"director-film 按钮与烂片案例共用 6068516 导致 5 个 ID"问题已修复 |
| `test:js` | ✅ 8/8 |
| HTTP 抽查 | ✅ index.html / dialect_aggregates / frontend_dataset / particles / narrative_facts / story_universe / build 产物 200 |
| 页面硬编码 | ✅ 首页无硬编码 v4.6 数字（3,067/9,724/79 部等均动态加载自 JSON） |

---

## 五、文档一致性：README 残留 5 处（低风险、提交前应清理）

定义文档《方言电影定义_最终版_v4.1_Final.md》已同步 v4.7 ✓；README 核心发现表已 v4.7 ✓，但残留：

| 位置 | 现状 | 应改为 |
| --- | --- | --- |
| L139 | "61 项数据、叙事和页面测试通过" | **66 项** |
| L153 | "6 个幂等 apply 脚本" | **9 个**（replay 链实际 9 个脚本） |
| L249–252 | 脚本表 4 行标 "v4.6：" | v4.6/v4.7 或 v4.7 |
| L256 | replay 链描述 "v4.6 语言回填" | 补 "v4.7（部分抓取终态）" |
| L263 | 补丁链描述缺 v4.7 | 同上 |

另：`sample_manifest.json` 顶层 `generated_at` 仍为 2026-08-18（补丁脚本只更新指纹未更新时间戳，低风险溯源瑕疵）。

---

## 六、版本管理与发布：P0 高压（今日截止）

1. **未提交改动 81 个文件**：v4.7 数据（narrative / dialect_aggregates / frontend_dataset / particles / geo_enrichment / story_universe / aggregated_stats / 64 详情分片 / report_data_strict）+ 脚本（freeze_constants / replay / verify / extract_narrative / gen_dialect_report）+ 测试 + README + 定义文档。这是 v4.7 的全部，**必须一次性提交**。
2. **未推送**：`cursor/archive-volume-pages` 领先 origin/main 2 个提交（0de5b16 归档卷页 + f860fc3 首页文案/语言回填），本地无对应远端分支。
3. **HEAD 不一致**：见第三节——当前 HEAD 在 CI 上必红，提交 v4.7 整包后即恢复全绿。
4. **部署配置就绪**：`vercel.json`（静态部署、`/` rewrite → `frontend/index.html`）无需构建命令；CI `.github/workflows/validate.yml` 在 push main / PR 时执行 rebuild → 测试 → freeze → JS 检查 → 构建确定性 diff。
5. **og:image 为相对路径** `assets/og-cover.jpg`（构建时未设 `SITE_URL`）——社交分享（微信/微博）可能取不到图；建议部署时以 `SITE_URL` 重建或确认平台行为。

---

## 七、卫生问题（低风险）

- `data/cleaned/_probe2.html`（3KB）未跟踪残留，为豆瓣抓取探针 HTML——**提交前删除**或补 gitignore（现有 ignore 只覆盖 `_*.json`/`_*.txt`）。
- `方言片详细报告.html` 最后提交于 f860fc3（可能基于 v4.6 快照生成）；属辅助审计文档，建议确认是否需随 v4.7 重新生成。
- `.gitignore` 覆盖良好（node_modules / .venv / data/source / 抓取缓存 / docs/history），无大文件误提交。

---

## 八、优先级行动清单

**P0（今日，提交前）**
1. 清理 README 5 处残留（61→66 项、6→9 个脚本、v4.6→v4.7 标签、两处补丁链描述）。
2. 删除 `data/cleaned/_probe2.html`。
3. 整包验证一次：`unittest discover -s tests`（66 OK）+ `verify_freeze_readiness.py`（20/20）+ `npm run check`（全绿）。
4. **一次性提交全部 81+ 个文件**（数据 + 脚本 + 测试 + README + 定义文档），提交信息注明 v4.7 终态。
5. 推送 `cursor/archive-volume-pages` → 合并 main（触发 CI）→ 确认 CI 全绿。
6. 确认 Vercel 线上生效（`/` → index.html 200、移动端 + 低速网络抽查）。

**P1（提交后）**
7. 以 `SITE_URL` 重建 og:image 绝对地址（或部署平台侧处理），验证微信/微博分享卡片。
8. 154 部分层抽检若来不及看片，在方法页说明抽样审计状态即可，不阻塞提交。

---

## 九、验证记录（本次评审实际执行的命令）

- `git status / branch / log / diff --stat` → 基线 81 个未提交文件、2 个未推送提交
- `python scripts/verify_freeze_readiness.py` → 阶段 B 20/20 PASS
- `python -m unittest discover -s tests` → 66 tests OK（首跑 5 失败系并发修改，复跑确认）
- `npm run check` → check:js / check:budget / check:contracts 全绿
- `npm run test:js` → 8/8 通过
- `python serve.py --port 8766` + curl → 关键资源全部 200
- 指纹核对：manifest / narrative_facts / aggregated_stats / dialect_aggregates / geo_enrichment / story_universe / details 七处均为 `70addfb0d7b53edbdc0d26002b4f50d217902fdf6f0db5e405eb8236ee7c207e`
- README/定义文档 diff 逐条核对 v4.6→v4.7 残留

---

## 十、深度质量审查（08-30 第二轮补查：数据清洗 / 前端设计 / 文案 / 样式）

在验证矩阵全绿之上，本轮逐层检查了数据清洗质量、前端设计与文案-数据一致性，共发现 **2 项实质问题（P1）与 4 项文案过期（其中 1 项 P0 级）**。

### 10.1 数据清洗：整体干净，2 项实质问题

**干净面（实测）**
- 63,025 行：必填字段 0 缺失、movie_id 0 重复、评分/年份/评价人数 0 越界、Decade 与年份 0 不符、乱码 0。
- **方言判定全表可重算**：用 `language_code(语言, Region)` 重算 63,025 行，770 行与 CSV 不一致——全部有据可查（TIER2B 354 + AUDIT 70 + LANG_BACKFILL_TIER2B 19 + Wikidata"不铸造方言"327），**0 行不可解释**。
- 含方言标签但 Is_Dialect=0 的 826 行：373 部 Tier2b 默认排除、49 部戏曲/演唱会、21 部审计、19 部回填 Tier2b、327 部 Wikidata 韩语/朝鲜语（按外语归组）、55 部方案 A（China 外语排首）——逻辑自洽。
- EMPTY_LANG_DEFAULTED 1,453 行与 China 空语言=0 不变量一致；无对白+方言 0 冲突。

**🔴 问题 1（P1）：《椒麻堂会》(27305997) 被漏判为非方言**
- 2021 年、豆瓣 **8.5 分 / 83,560 人评价**，语言字段「四川乐山话」——方言电影代表作之一。
- 根因：`dialect_defs.py` 白名单缺「四川乐山话」（西南官话组只有四川话/四川方言/重庆话等，无乐山下位变体）。回填后 classify 不识别 → Is_Dialect=0、Language_Code=5、戳 EMPTY_LANG_DEFAULTED。
- 影响：2020s 中国方言片 88 → 应 89 部；方言片均分/烂片率微调（8.5 高分片加入只会强化"守住下限"结论）；但**代表性方言片缺席 2020s 片单是编辑层面明显遗漏**。
- 修复：白名单增补「四川乐山话」「乐山话」（数据集仅 1 行命中）→ 重跑回填/重算 → 更新 freeze 基线。**注意：这会导致 v4.7 终态数字变化，需在提交前决定是否处理。**

**🔴 问题 2（P2）：《万千星辉颁奖典礼 2020》(35322591) 被误收为 2020s 方言片**
- 2021 年 TVB 颁奖典礼、粤语 5.8 分，被 v4.6/v4.7 回填 classify 为 Tier 1/2a 方言片。
- 按 E8（颁奖典礼）排除原则应排除；但 opera/audit 名单是 2026-08-18 快照（49 部），未覆盖回填批次新片。
- 影响：2020s 方言片 88 → 应 87。与问题 1 一增一减恰好抵消总数，但**成分不同**，mean/below5 仍会微变。

**🟡 其他（P3）**
- 语言字段噪声：《蓝色防线》(33464837)「努维尔语」来源可疑（不影响判定）。
- 2 部「无对白」+ 1 部「日语」被戳 EMPTY_LANG_DEFAULTED（戳记语义偏差，计数无影响）。
- 方案 A 排除 56 部无行级 `Dialect_Evidence` 标记（数字在 freeze 冻结，但 CSV 不可见，可追溯性小瑕疵）。
- `sample_manifest.json` 顶层 `generated_at` 仍为 08-18（补丁未更新时间戳）。

### 10.2 前端设计：无布局冲突，结构清晰

- 场景系统：12 个 step（封面/引言/格局×2/发现×3/反证/同导演/三波/五维/终章）对应十幕+终章，`data-scene` 动态装配 scene-lab；markLine 恰 8 处（测试锁定 8 ✓，08-27 的 10vs8 已收敛且核实非回归）。
- 层级与交互：z-index 1/10/18/25/30/50 分层有序；scrolly-overlay `pointer-events:none` + 卡片 `pointer-events:auto` 恢复，粒子点选与滚动叙事不冲突。
- 无障碍：skip-link、role=alertdialog、aria-live/expanded 齐全；`prefers-reduced-motion` 6 处 + `prefers-reduced-transparency` 1 处。
- 数据全部动态加载（dialect_aggregates / narrative_facts / frontend_dataset / wave_cases），正文无 v4.6 硬编码残留。
- 🟡 仅 2 处过期 fallback：app.js L135 普通话烂片率 fallback `24.4%`（应 24.5%）、方言 `6.4%`（应 6.5%）——仅 JSON 加载失败时兜底显示，低风险但建议顺手改。
- 🟡 index.html L465-514 filter-bar 缩进错乱（功能无碍，整洁度问题）。

### 10.3 文案：正文与数据一致，方法区 1 处 P0 过期

**✅ 正文核心数字全部核实无误**
- 同导演案例：立春 8.3 / 微爱之渐入佳境 4.5、隐入尘烟 8.4 / 路过未来 6.6、大话西游之大圣娶亲 9.2 / 东成西就2011 4.0、狗十三 8.2 / 烈日灼心 8.2 / 脱缰者也 5.2——与数据逐部一致。
- 烂片案例 4 部（3.0/2.9/4.0/4.6）与数据一致；flopCaseIds 契约 4 唯一值 ✓。
- 高分率（≥8.0）方言 9.5% < 普通话 11.9%、"没有赢在最高分"表述正确。
- 2021+ China 8.0+ 恰 121 部（L544 准确）；2022+ 7,190 部、China 15.4%（L543 准确）。

**🔴 P0 文案过期：方法区「语言字段完整性」整段与 v4.7 数据矛盾（L542）**
- 原文："2021 年起批次（最终入样 9,541 条）语言字段完整率为 **0%**，一律默认为「汉语普通话」。因此 2021–2026 的 **1,631 部**中国电影**全部计入普通话组，不参与方言／普通话对比**。"
- 事实（v4.7）：2021–2026 中国电影确为 1,631 部，但其中 **21 部方言片**（粤语×17、藏语、闽南语、《隐入尘烟》2022 甘肃方言、《给阿嬷的情书》2026 潮汕话）已参与对比——**与正文叙事（隐入尘烟 8.4、给阿嬷 9.3 两个核心案例）直接矛盾**。
- 修复方向：改写为"2021+ 批次已按豆瓣「语言」回填（2,385 条）；抓取失败者默认普通话并戳 EMPTY_LANG_DEFAULTED；仍有 1,453 行语言标注为默认普通话"。

**🟡 其他文案（P3）**
- L542 "约 240 部"（普通话组含其他语言条目）→ 实际 **346 部**（155 无对白 + 191 英语/其他），应更新。
- L544 "相当比例为演唱会、线上演出与综艺影像"（10/121 ≈ 8%）表述略夸，可改为"少量"。
- L401 正文案例数字均精确，无需改。

### 10.4 样式统一性：token 化良好，少量不彻底

- ✅ 颜色/字体/字号/行高/间距/圆角全面 token 化（:root 71 个变量）；3 套字体栈（Noto Serif SC 标题 / Noto Sans SC 正文 / Outfit 数字）；6 个响应断点（1024/900/820/768/720/700/520）。
- ✅ 深色沉浸主题统一（#050507 底），地区色 7 组 token（china 金黄 #d4a574）。
- 🟡 非 token 硬编码色 20+ 处（灰阶 #C8C8CC/#8E8E96/#A5A5AC 系、金黄变体 #F0D7A8/#E8C07A、珊瑚 #FF7A73）——视觉一致但改主题时需逐处改，建议收敛。
- 🟡 1 处 `!important`（`.theory-badge` padding 7px 13px，L316）——存在基类竞争，建议消除。
- 🟡 卡片语言不完全统一：dim-card（半透明内嵌）vs director/flop-card（暗底浮起）——设计意图可辩护，但五维卡与案例卡观感不同。
- ✅ `.volume-page` 样式保留是有意为之（archive 卷页复用主站 CSS，README 已说明）。

### 10.5 深度审查结论

- **数据层的"软质量"与"硬验证"同样干净**：方言判定可重算 0 不可解释、排除链自洽、案例数据零差错——这是项目数据可信度的根基，经得起抽查。
- **但 3 个具体问题需要在提交前决策**：①《椒麻堂会》白名单缺口（P1，改则 v4.7 终态数字变化）；②《万千星辉颁奖典礼》误收（P2，E8 应排除）；③方法区 L542 整段过期（P0，必改，否则读者会误以为 2021+ 无方言数据，与正文案例矛盾）。
- 若①+②都处理，2020s 方言片 88→89→88（成分变化），mean/below5 微变，需同步 dialect_aggregates + freeze 基线 + 测试期望；若时间不允许，至少修复③，并将①②记入方法区"待核查"。
