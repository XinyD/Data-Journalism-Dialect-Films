# 方言电影交互网页 · 整体架构与设计方案

> **历史交互蓝图，非正式现行数据口径。** 现行发布基线以 `README.md`、`scripts/freeze_constants.py` 与 `data/frontend/dialect_aggregates.json` 为准（v4.5：China **12,791** / 普通话 **9,746** / 指纹 `ecd258a80e…`）。下文 12,858 / 9,813 / `c64962992e` 等为撰写时的 v4.4 快照，请勿当作现行事实。
>
> 本文档为《方言电影为什么"赢"了？》交互网页的**完整架构与开发蓝图**。  
> 基于现有仓库（原生 JS + ECharts + Canvas 粒子引擎，纯前端静态站）重构为**以方言为绝对主线**的数据新闻作品。  
> 数据底座：`derived_movies.csv`（63,025 部，已规范化）；前端载荷：`data/frontend_dataset.json`。
>
> **⚠ 数据基线声明（2026-08-19 定稿，历史快照）**：本文档正文数字撰写时对齐 **v4.4 冻结基线**：总行数 **63,025**、Region=China **12,858** 部、方言片 **3,045** 部（Tier 1 **2,289** / 2a **410** / 2b **346**）、普通话·非方言 **9,813** 部、全量方言 3,338、指纹 c64962992e。现行数字以 README / freeze_constants / dialect_aggregates 为准。开发期前端正文数字一律从载荷动态加载，禁止手工硬编码。

---

## 0. 一句话核心定位（先给结论）

**推荐表述：**

> 方言电影为什么"赢"了？——当普通话工业化量产稀释了品质，选择方言的创作者以更自觉的差异化守住了华语电影的下限。但这"赢"不是方言的赢，是好故事赢了同质化——而好故事需要土壤，中国辽阔的土地、上百种语言、丰富的风土人情，本就是创作最深厚的养分。今天我们在平均分上仍落后欧美，而那道鸿沟是质量的鸿沟，不是文化的鸿沟。

**备选（更短，适合封面副标题）：**

- A：「在主流产能同质化的时代，方言电影凭'差异化创作'逆势赢回口碑——这背后是一场内容自觉与文化自信的微观实践。」
- B（更锋利）：「方言没赢，赢的是好故事。普通话片用体量换来了最高的烂片率，方言片用更少的产量守住了更稳的下限。而好故事的土壤，是我们从不缺的东西。」

**核心切入点概括：** 我们不讲"方言电影更好看"，而是讲——**在普通话工业产品高速量产、稀释品质的时代，方言成为一道"品质筛选机制"，让华语电影里口碑最稳、下限最高的一块，恰恰是用更少数量换来了更高关注度的方言创作。** 但方言不是答案，创作态度才是；好故事赢了同质化，而文化多样性的土壤滋养了好故事。落点不挑动方言与普通话的对立，也不声称超越西方——我们只是讲质量，靠质量去赢；中国文化多样性的富矿，本就给了电影追上欧美的资本。

---

## 1. 项目背景与定位

| 项    | 内容                                        |
| ---- | ----------------------------------------- |
| 作品形态 | 滚动叙事交互网页（数据新闻）                            |
| 参赛赛道 | 主投「网页交互」赛道；若契合议题可叠加「中国故事」公众议题             |
| 数据规模 | 63,025 部电影；China 口径方言 3,045 部、普通话·非方言 9,813 部   |
| 核心论点 | 方言"赢"= 口碑/品质维度的胜出（相关 + 选择偏差），非因果"方言让电影更好" |
| 落点   | 赢的是同质化；差异化创作 = 文化自信的微观实践                  |
| 诚实边界 | 数据集无票房/成本；外部参照仅支撑"下限稳"共性，不支撑"均分反转"因果      |

---

## 2. 技术架构总览

### 2.1 技术栈（沿用现有，零新增依赖）

- **前端**：原生 HTML/CSS/JS（ES Module 化），无构建步骤
- **可视化**：Canvas 自研粒子引擎（电影粒子重排）+ ECharts 5.5（`vendor/` 本地引入）
- **数据**：`data/frontend_dataset.json` 全量加载到浏览器端（已存在管线）
- **托管**：`serve.py`（`http.server` 封装）；参赛需公网部署（CloudStudio / EdgeOne Pages）

### 2.2 目录结构（目标态）

```
movie-rating-data-story-main/
├── data/
│   ├── derived_movies.csv          # 原始规范数据（63,025，v4.4 冻结）
│   ├── frontend_dataset.json       # 前端载荷（新增方言维度聚合）
│   ├── narrative_facts.json        # 叙事事实（复用+扩展）
│   └── sample_manifest.json
├── pipeline/
│   ├── config.py                   # 聚合配置（新增方言/语言分层聚合）
│   └── rebuild.py                  # 生成 frontend_dataset.json（确定性）
├── frontend/
│   ├── index.html                  # 封面 + 滚动叙事 + 终章 + 探索舱入口 + 方法论
│   ├── style.css
│   ├── js/
│   │   ├── core.js                 # 数据服务 + 详情弹窗（复用）
│   │   ├── app.js                  # 主控制器：滚动场景、目录、交互调度（重构文案+控件）
│   │   ├── particle.js             # 粒子引擎（复用+方言着色）
│   │   ├── scenes/                 # 各叙事幕模块（新增，按幕拆分）
│   │   │   ├── scene_arc.js        # 第一幕 逆转弧线
│   │   │   ├── scene_collapse.js   # 第二幕 产量塌陷
│   │   │   ├── scene_threshold.js  # 第三幕 门槛效应
│   │   │   ├── scene_global.js     # 第四幕 外部参照
│   │   │   └── scene_waves.js      # 第五幕 三波浪潮
│   │   └── explore/                # 探索舱四卷（重构为方言核心）
│   │       ├── vol_time.js         # 卷一 年代弧线
│   │       ├── vol_geo.js          # 卷二 地区/语言对照
│   │       ├── vol_lang.js         # 卷三 语言构成
│   │       └── vol_filter.js       # 卷四 交叉筛选
│   ├── explore.html                # 探索舱容器（或同页分区）
│   ├── method.html                 # 方法论 / 数据边界页
│   └── vendor/                     # ECharts 等
└── serve.py
```

### 2.3 数据流水线（新增方言聚合）

在 `config.py` / `rebuild.py` 中新增的聚合维度（基于 `Is_Dialect`、`Language_Category`、`Region`、`Decade`）：

1. 方言 vs 普通话的**分年代**均分 / 烂片率 / 高分率 / 单片评价数；
2. **类型控制**三口径：原始 / 剔除演唱会·纪录 / 仅剧情片；
3. 粤语 vs 非粤语方言对比；
4. **外部参照三组**：欧洲 `English` vs `European_Languages`、日韩 `Japanese_Korean`、北美 `English` 基准；
5. 逐年交叉点序列（2000–2015）；
6. 三波浪潮案例集（港片粤语 / 西南方言 / 闽南语新浪潮）的精选片单。

> 所有聚合须**预计算落盘**，禁止前端随机采样——保留 `rebuild.py` 的 `git diff --exit-code` 确定性承诺。

### 2.4 设计 Token 体系（CSS 变量规范）

在 `style.css` 的 `:root` 中统一定义以下 Design Token，确保全项目视觉语言一致：

**色彩 Token**

| Token 名称 | 色值 | 用途 |
|-----------|------|------|
| `--color-amber` | `#d4a574` | 方言主色（琥珀色） |
| `--color-amber-dim` | `rgba(212, 165, 116, 0.15)` | 琥珀色半透明底 |
| `--color-slate` | `#8899aa` | 普通话主色（灰蓝色） |
| `--color-slate-dim` | `rgba(136, 153, 170, 0.15)` | 灰蓝色半透明底 |
| `--color-danger` | `#E53935` | 烂片警示/危险红 |
| `--color-surface-1` | `#0d0d0d` | 一级表面（最深） |
| `--color-surface-2` | `#1a1a1e` | 二级表面（卡片底） |
| `--color-surface-3` | `#252528` | 三级表面（悬浮元素） |
| `--color-text-high` | `#FFFFFF` | 一级文字（标题） |
| `--color-text-mid` | `#E2E2E2` | 二级文字（正文） |
| `--color-text-low` | `#888888` | 三级文字（辅助说明） |

**间距 Token**

```css
--space-xs: 4px;
--space-sm: 8px;
--space-md: 16px;
--space-lg: 32px;
--space-xl: 64px;
--space-2xl: 120px;
```

**圆角 Token**

```css
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-pill: 999px;
```

**阴影 Token**

```css
--shadow-card: 0 16px 36px rgba(0, 0, 0, 0.34);
--shadow-dialog: 0 24px 64px rgba(0, 0, 0, 0.6);
--shadow-tooltip: 0 8px 24px rgba(0, 0, 0, 0.5);
```

**动效 Token**

```css
--ease-out-quint: cubic-bezier(0.22, 1, 0.36, 1);
--ease-in-out-cubic: cubic-bezier(0.65, 0, 0.35, 1);
--duration-fast: 200ms;
--duration-normal: 400ms;
--duration-slow: 800ms;
```

**层级 Token**

```css
--z-particle: 1;
--z-overlay: 10;
--z-nav: 50;
--z-dialog: 100;
--z-toast: 200;
```

---

## 3. 功能模块规划（五层）

### 3.1 数据层

- 载入 `frontend_dataset.json`（含上述新增聚合）；
- 提供查询 API（内存索引）：按语言/年代/类型/地区筛选、分桶统计、Top 列表；
- 维护"诚实边界"元数据（字段缺失、混杂、误标清单），供方法论页与弹窗脚注调用。

### 3.2 渲染层

- **粒子引擎（Canvas）**：每部电影 = 一个粒子，按当前幕的比较维度做位置/颜色/大小映射（方言 vs 普通话双色；评分映射亮度；评价数映射大小）。复用现有引擎，新增"方言高亮""混杂类型弱化"模式。
- **图表层（ECharts）**：折线（逆转弧线）、柱状（烂片率对比）、箱线/分布（门槛效应）、条形（跨国对照）、时间轴/聚类（三波浪潮）。复用现有 `vol*.js` 图表组件。
- **DOM 叙事层**：每幕文案卡片、数据金句、钩子句，按滚动进度淡入。

### 3.3 交互层

- 滚动驱动（IntersectionObserver 触发幕切换 + 粒子重排）；
- **比较控件**（每幕一个）：切换口径 / 拖动切点 / 筛选语言；
- 悬停粒子 → 浮层显示片名+评分；点击粒子 → 详情弹窗；
- 侧边章节目录（锚点跳转 + 进度指示）；
- 探索舱深潜（标签页 / 分页）；
- "捞一部"随机按钮（随机展示一部方言片，呼应"随机打捞"）。

### 3.4 内容层

- **首页叙事**（封面 + 序章 + 五幕 + 终章）；
- **探索舱**（四卷，以方言为核心重构）；
- **方法论 / 数据边界页**（诚实清单 + 外部参照边界 + 票房缺失说明）。

### 3.5 导航与部署层

- 顶部/侧边导航：首页 / 探索舱 / 方法论；
- 移动端响应式（**现有版本缺失，须新增**：粒子降密度、图表改纵向、控件常显）；
- 部署脚本 + 投稿用公网 URL 说明。

---

## 4. 页面结构与信息架构

```
┌──────────────────────────────────────────────────┐
│  封面：粒子星云 + 标题 + KPI 卡片                  │
├──────────────────────────────────────────────────┤
│  引言：《给阿嬷的情书》——从感性现象到数据追问        │
├──────────────────────────────────────────────────┤
│  序章：数据交互验证烂片率反差 + 高分率纠偏 + 年代线索│
├──────────────────────────────────────────────────┤
│  第一幕  逆转弧线    （时间维度：30年反转）         │
│  第二幕  产量塌陷    （普通话：烂片率24.4%）        │
│  第三幕  门槛效应    （下限更稳 + 个案对比）        │
│  第四幕  外部参照    （跨国：小语种下限稳共性）      │
│  第五幕  破·方言也有烂片 → 立·同导演对比 → 展·三波浪潮  │
├──────────────────────────────────────────────────┤
│  终章  好故事赢了同质化 + 文化多样性的土壤          │
├──────────────────────────────────────────────────┤
│  探索舱（四卷）                                   │
│   卷一 年代弧线 │ 卷二 地区对照 │                  │
│   卷三 语言构成 │ 卷四 交叉筛选                    │
├──────────────────────────────────────────────────┤
│  方法论 / 数据边界页（含 scale-tag 标注体系）       │
└──────────────────────────────────────────────────┘
        ↑ 滚动叙事（被动+交互验证）│ 点击导航 / 返回
```

**页面清单表**

| 页面/分区   | 类型     | 入口        | 主要交互            |
| ------- | ------ | --------- | --------------- |
| 封面      | 首屏     | 默认        | 向下滚动 / 进入按钮     |
| 引言      | 叙事过渡   | 滚动        | 滚动进入序章            |
| 序章 + 五幕 | 滚动叙事   | 滚动        | 滚动翻幕、比较控件、悬停、点击 |
| 终章      | 滚动叙事收束 | 滚动        | 进入探索舱按钮         |
| 探索舱（四卷） | 独立页/分区 | 终章按钮 / 导航 | 标签页切换、图表联动、筛选   |
| 方法论页    | 独立页    | 导航 / 页脚   | 阅读、锚点           |

---

## 5. 内容分区 · 逐幕详细设计

> 每幕包含：定位 / 讲述的故事 / 关键数据点 / 可视化方案 / 交互设计 / 视觉与情感提示 / 前后衔接钩子。

### 5.0 封面（钩子）

- **定位**：3 秒内用视觉氛围和核心问题抓住注意力——只制造好奇心，不给结论。

- **可视化**：
  全屏 63,025 个电影粒子构成缓慢漂浮的星云，方言粒子以暖色（琥珀）
  点缀其中。粒子仅做缓慢漂浮，不做聚合文字的复杂动画。

- **交互**：
  - 粒子星云漂浮 1–2 秒后，主标题与副标题淡入出现；
  - KPI 卡片随滚动逐步淡入；
  - 右下角常驻「进入故事 ↓」按钮。
  - **首屏状态**：深色背景 + 粒子星云 + 大标题 + 副标题 + 滚动提示，
    保持极简，不让信息过载。

- **视觉**：深色背景（#0d0d0d）+ 暖色方言粒子（琥珀 #d4a574）+
  冷色普通话粒子（灰蓝 #8899aa）作背景底色；衬线大标题（思源宋体）。

- **内容顺序**（从上到下）：
  1. 粒子星云（始终漂浮，不做形态变化）
  2. 主标题：「方言电影为什么"赢"了？」
  3. 副标题（核心定位金句）：
     > ——当普通话工业化量产稀释了品质，选择方言的创作者以更自觉的差异化守住了华语电影的下限。
  4. KPI 卡片（4 个大数字卡片，滚动淡入）：
     - **63,025** 部电影
     - **1888—2026** 年份跨度
     - **≥100** 每部最低评价人数
     - **12,858** 其中中国电影

- **衔接钩子**（滚动至底部时淡入）：
  「但这背后，是一条画了三十年的弧线。」

- **用户旅程 · 封面 → 引言阶段**：

  | 心理状态 | 行为 | 设计支撑 |
  | --- | --- | --- |
  | "这是什么？" → "方言电影为什么赢了？" | 被视觉氛围和标题抓住，产生好奇 | 粒子星云 + 主标题 + 副标题 + KPI 卡片 |
  | "凭什么？" → 带着追问进入引言 | 滚动 / 点击「进入故事」 | 衔接钩子引导向下 |

### 5.0b 引言：一部电影的数据追问

- **定位**：从感性现象过渡到理性数据命题。以《给阿嬷的情书》为叙事起点，
  让评委先「关心」一部电影，再「关心」六万部电影的数据。

- **叙事引子 ·《给阿嬷的情书》**（毛玻璃卡片，滚动淡入）：
  > 2026 年，一部没有头部 IP、没有流量明星的闽南语（潮汕话）家庭电影，
  > 让全国观众在影院里落泪。
  > 它叫《给阿嬷的情书》。
  >
  > 在 IP 和流量统治的时代，一个「小而真」的故事凭什么赢？
  >
  > 这个问题，将贯穿整个叙事。

  **设计意图**：
  - 主语是「一个故事」而非「方言」——先建立「以小博大」的反差感，
    再自然引出方言这个变量，避免「突然提起方言」的突兀感。
  - 核心问题「凭什么赢？」既是引言的钩子，也是整场叙事论证的终点——
    读者带着这个问题滚动下去，到终章才能获得完整回答。

- **诚实边界声明**（引子卡片下方，小字标注，scale-tag 标注「数据可证」）：
  > *注：《给阿嬷的情书》（2026）已在本作品数据集内，
  > 并计入方言片（语言字段潮汕话居首，Tier 2a）。
  > 原始交付数据缺语言列，该行曾被默认回填为“汉语普通话”而误归普通话组，
  > 2026-08-19 对照豆瓣实页（语言：潮汕话 / 汉语普通话 / 泰语 / 英语）修正补收——
  > 它是交付元数据缺漏与数据溯源修复的一个案例。
  > 下文所有数据均来自 63,025 部电影的实际核算，不引用该片具体评分。*

  **设计意图**：主动展示方法论自觉——评委看到这一行，信任度上升而非下降，
  因为「我知道数据的边界在哪里」本身就是数据素养的体现。
  与方法论页诚实清单形成双重锚点。

- **衔接钩子**（引言末尾，滚动淡入）：
  > 为了回答「凭什么赢？」，我们翻开了 63,025 部电影的数据——
  > 然后发现了一个没人说过的反常。
  >
  > ↓ 向下滚动，看看这个反常是什么 ↓

  **设计意图**：从具体电影（感性）→ 到宏观数据（理性）→ 到「反常」（好奇心）。
  评委带着「什么反常？」的问题进入序章，
  序章的粒子散点图将直接回应这个追问。

- **视觉**：
  纯文字 / 毛玻璃卡片，不需要粒子交互。粒子星云仍在背景中缓慢漂浮，
  但前景只有文字卡片，保持克制的氛围。

- **交互**：
  - 引言卡片滚动淡入（复用 `.particle-step` 的 `opacity` + `translateY` 动画）；
  - 评委读完后可继续滚动或点击「进入故事 ↓」进入序章。

- **UI 视觉规格**：

  **毛玻璃卡片样式**

  ```css
  backdrop-filter: blur(16px) saturate(1.4);
  background: rgba(13, 13, 13, 0.55);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-lg); /* 12px */
  max-width: 560px;
  padding: 32px 36px;
  box-shadow: var(--shadow-card); /* 0 16px 48px rgba(0,0,0,0.4) */
  ```

  **排版层级**

  | 元素 | 字体 | 字号 | 字色 | 行高 | 备注 |
  |------|------|------|------|------|------|
  | 引子正文 | 思源宋体 | 1.1rem | `#D7D7DB` | 1.8 | 常规字重 400 |
  | 「它叫《给阿嬷的情书》」 | 思源宋体 | 1.1rem | `#d4a574` | 1.8 | 加粗 700，琥珀色强调 |
  | 「凭什么赢？」 | 思源宋体 | 1.3rem | `#FFFFFF` | 1.6 | 居中，上下留白 24px |
  | 诚实边界声明 | 思源黑体 | 0.78rem | `#888888` | 1.6 | 左侧 2px 琥珀色竖线 |

  **诚实边界声明样式**

  ```css
  border-left: 2px solid var(--color-amber);
  padding-left: 12px;
  font-size: 0.78rem;
  color: var(--color-text-low);
  font-family: var(--font-sans);
  font-style: italic;
  ```

  **滚动淡入参数**

  | 参数 | 值 |
  |------|-----|
  | 触发方式 | IntersectionObserver |
  | 触发阈值 | `threshold: 0.25` |
  | 初始状态 | `opacity: 0; transform: translateY(32px) scale(0.97)` |
  | 激活状态 | `opacity: 1; transform: translateY(0) scale(1)` |
  | 缓动曲线 | `var(--ease-out-quint)` — `cubic-bezier(0.22, 1, 0.36, 1)` |
  | 时长 | `600ms` |

  **阅读节奏控制**

  | 元素间距 | 留白高度 | 设计意图 |
  |---------|---------|----------|
  | 引子卡片 → 诚实声明 | `min-height: 30vh` | 停顿思考 |
  | 诚实声明 → 钩子文案 | `min-height: 40vh` | 情绪沉淀 |
  | 钩子文案 → 序章起始 | `min-height: 20vh` | 过渡准备 |

  **背景粒子透明度控制**

  ```css
  /* 引言区域粒子引擎保持运行，但降低全局透明度确保文字可读性 */
  .scene-intro canvas#chart-container {
    opacity: 0.4;
    transition: opacity 0.6s ease;
  }
  ```

- **用户旅程 · 引言阶段**：

  | 心理状态 | 行为 | 设计支撑 |
  | --- | --- | --- |
  | 「哦，这个我知道」→「凭什么？」 | 被《阿嬷》故事打动，从好奇进入追问 | 叙事引子 + 诚实边界声明 |
  | 「什么反常？」→ 带着追问向下滚动 | 滚动进入序章 | 引言钩子（「没人说过的反常」） |

### 5.1 序章：一个问题

- **定位**：承接引言的数据追问（「凭什么赢？」），将其转化为**可交互验证的数据命题**。
  评委不是"被告知"结论，而是通过粒子散点图**亲眼看到**反差，
  在交互中**自己发现**纠偏信息和时间线索。

- **开篇 · 数据反差揭示**（滚动触发，粒子从封面星云过渡为散点图）：
  - 粒子从全屏星图过渡为"普通话 vs 方言"评分散点图
    （复用 `prologue-rift` 场景，X 轴 = 语言组，Y 轴 = 评分）。
  - 引导线系统同时激活（复用 `createGuideMarkLine`）：
    - 5.0 烂片线（红色虚线）
    - 普通话均分线 6.11（蓝色虚线）
    - 方言均分线 6.62（金色虚线）
    - 均分差色带（`horizontalDifferenceBand`）
  - 烂片带高亮（`markArea`，Y 轴 0–5 区间，红色半透明）：
    普通话粒子在烂片带内密密麻麻，方言粒子寥寥无几。
  - 前景层浮现大字数字：**6.4%** 与 **24.4%**。

  **设计意图**：封面抛出"凭什么赢"的问题后，序章不给文字答案，
  而是让评委**亲眼看到**——烂片带内蓝色粒子的密集 vs 金色粒子的稀疏，
  比任何文字都有说服力。

- **交互**（scene-lab 面板，与后续八幕结构完全一致）：
  - **数据卡**（`.scene-metrics`，4 列网格）：

    | 普通话均分 | 方言均分 | 均分差 | 烂片率差 |
    |-----------|---------|-------|--------|
    | 6.11 | 6.62 | +0.51 | −18.0pp |

  - **年代切换按钮**（`.scene-option`）：
    `全部年份` `1990s` `2000s` `2010s` `2020s`
  - **烂片阈值滑块**（`input[type="range"]`）：
    默认 5.0，范围 3.0–8.0。拖动时烂片带区域和烂片率数字同步变化。
  - **随机打捞**（`.scene-random`）：
    从 3,045 部方言片中随机选一部 → 弹出详情弹窗。
  - **insight 文本**（`.scene-insight`）：
    > 全部年份：方言减普通话为 +0.51 分。烂片率差 −18.0 个百分点。
    > 普通话片每四部就有一部踩雷，方言片每十六部才有一部。

  **设计意图**：序章的 scene-lab 面板与后续八幕**完全同构**——
  评委在序章中学到的每一个交互范式（药丸按钮、滑块、随机打捞），
  在后续八幕都能直接使用。序章就是"第零幕"。

- **高分率纠偏**（融入数据交互，非独立卡片）：
  在 scene-lab 面板中新增一组数据卡（复用 `.scene-metrics` 样式）：

  | 方言高分率 | 普通话高分率 | 备注 |
  |-----------|------------|------|
  | 9.5% | 11.8% | 方言高分率反而更低！ |

  insight 文本：
  > 等等——方言的高分率（≥8 分）9.5%，其实略**低于**普通话 11.8%。
  > 方言的胜出在**地板**（烂片率低），不在**天花板**（高分率高）。
  > 类型控制后（仅剧情片）方言高分率才反超（14.4% vs 8.5%）。

  **设计意图**：评委不是"被告知"高分率纠偏，而是在查看数据时**自己撞见**这个意外——
  "原来方言也不是全面碾压"。这让纠偏从"被动接受"变成了"主动发现"，
  记忆更深，信任度更高。

- **年代线索**（预埋第一幕"逆转弧线"的伏笔）：
  insight 文本引导评委切换年代按钮：
  > 切换年代按钮，看看这个差距是否随时间变化。

  当评委点击"1990s"时：
  > 1990s：方言减普通话为 **−0.39 分**。普通话反而领先。

  当评委点击"2010s"时：
  > 2010s：方言减普通话为 **+0.95 分**。方言大幅反超。

  **设计意图**：评委在序章就**自己发现**了"1990s 负、2010s 正"——
  这是第一幕"逆转弧线"的精确数据点。
  但评委只看到了两个年代的数据点，第一幕要展示的是**逐年交叉的完整弧线**。
  序章→第一幕的过渡变成"我看到了两个点"→"让我看完整的线"——好奇心是递进的。

- **衔接钩子**（触发式，评委切换过至少 2 个年代后显示）：
  > 1990 年代普通话领先，2010 年代方言大幅反超——
  > 这中间发生了什么？
  >
  > ↓ 向下滚动，拉开这条画了三十年的弧线 ↓

  视觉：粒子图短暂展示 1990s 与 2010s 的对比快照（左右分屏或淡入淡出），
  配合脉冲动画和滚动提示。

  **设计意图**：评委在序章中**自己发现了悬念**（"两个年代竟然相反"），
  而不是"被告知"有一个悬念。向下滚动的动力从好奇心而非指令中来。

- **叙事回声标记**（序章末尾，以小字/淡色呈现）：
  > *这个问题——「方言电影凭什么赢？」——将贯穿整个叙事。
  > 答案不在开头，在数据的终点。*

  **设计意图**：显式告诉读者"记住这个问题"，
  为终章的"回响"段落埋下伏笔，形成首尾呼应的叙事契约。

### 5.2 第一幕 · 逆转弧线（时间维度）

- **定位**：最反直觉、最有戏剧性的开场证据——但序章已预埋了伏笔。
- **承接**（衔接序章结尾钩子）：
  > 序章中，你已经看到两个年代的数据点——
  > 1990s 普通话领先 0.39 分，2010s 方言反超 0.95 分。
  > 但两个点只是一条线的两端。
  > 现在，让我们把这条线画完整。

  **设计意图**：评委在序章已经"看到两个点"，第一幕的任务不是"揭示反转"（那已在序章完成），
  而是"展开完整弧线"——评委将看到逐年交叉的连续曲线，
  发现交叉点不是突然出现的，而是从 2005 年前后首次转正、到 2010 年才形成结构性断点。
  拖动滑块时，每一个年份都是一个"发现"的时刻。
- **故事**：1990 年代普通话领先方言 0.39 分；2010 年代方言反超 0.95 分——30 年摆幅 1.34 分。逐年差在 2005 年首次转正，2010 年为结构性断点（当年方言 6.83 vs 普通话 6.29）。
- **关键数据**：逆转弧线表（1990 前 −0.31 / 1990s −0.39 / 2000s −0.33 / 2010s +0.95 / 2020s +0.59）。
- **可视化**：折线/面积图（方言 vs 普通话分年代均分），逐年交叉点高亮。
- **交互**：**拖动年份滑块**，实时看两组差距从"负"翻到"正"；悬停看当年代表片。
- **视觉**：冷（普通话）暖（方言）双色；断点处脉冲提示。
- **情感**："原来反转不是一夜之间发生的——它花了整整十年。"
- **衔接钩子**：「反转发生了，但它为什么发生？先看普通话这边出了什么。」

### 5.3 第二幕 · 产量塌陷（机制 A）

- **定位**：解释普通话为何落后——不是不好，是"太多太滥"。
- **故事**：普通话片烂片率 24.4%，2010 年代飙到 36.1%；海量供给（市场洪流，同年产量是方言片的 6.8 倍）把均值拽低。方言同期仅 13.4%。
- **关键数据**：2010s 烂片率普通话 36.1% vs 方言 13.4%（踩雷率 2.7 倍）；单片平均评价 2.3 万（普通话）vs 4.7 万（方言）。
- **可视化**：烂片率柱状（分年代）+ 数量/评价数气泡。
- **交互**：切换"全部 / 2010s"看塌陷幅度；点击高烂片率区看案例。
- **视觉**：灰暗下沉的柱状，强调"量越大、雷越多"。
- **衔接钩子**：「那方言这边，凭什么这么稳？」

### 5.4 第三幕 · 门槛效应（机制 B）

- **定位**：解释方言为何赢——下限更稳，是"被精选过的高品质子集"。
- **故事**：方言烂片率仅 6.4%，且**类型控制后结论仍成立**（剔除演唱会·纪录 6.58 vs 5.93；仅剧情片 6.86 vs 6.25、烂片率 5.2% vs 18.0%）。非粤语方言（7.22）甚至高于粤语（6.51）——不是港片独大。
- **关键数据**：烂片率 6.4% vs 24.4%（最硬一条）；类型控制三口径表。
- **低分/高分个案对比**（双栏卡片，让统计数据落地为观众认知的具体电影）：
  - **普通话低分个案**：上海堡垒(2.9)「特效+流量」/ 爱情公寓(3.2)「IP混搭」/ 何以笙箫默(3.8)「IP+流量」/ 三生三世(4.0)「仙侠特效」/ 诛仙1(4.5)「IP+流量」
  - **方言低分个案**：天机·富春山居图(3.0)「杂糅动作」/ 西游记之大闹天宫(4.4)「特效堆砌」/ 澳门风云3(4.5)「合家欢拼贴」/ 长城(4.9)「工业化特效」
  - **观察**：低分的共性与语言选择关系不大，而与**创作路径的选择**有关——普通话低分更集中于"IP+流量"模式，方言低分更集中于"合拍大片"模式。
- **可视化**：分布对比（箱线/小提琴）+ 类型控制三栏并排 + 个案对比双栏卡片。
- **交互**：**切换口径**（原始 / 剔除混杂 / 仅剧情片），让读者亲眼看到"结论不依赖干扰项"——这本身是方法论加分。
- **视觉**：方言分布"短尾巴"（低分尾薄），普通话"长尾巴"。个案卡片红色边框强调。
- **衔接钩子**：「这种'小语种更稳'，是华语独有，还是全球共性？」

### 5.5 第四幕 · 外部参照（格局拔高）

- **定位**：跨国验证共性 + 凸显华语特色，避免"方言高=通例"误读。
- **故事**：在欧洲片内部，非主导语言（European_Languages）烂片率 1.5% 也低于主导语言英语 3.6%——"小语种下限稳"是全球共性。但华语普通话烂片率 24.4% 是**全球异常值**（欧洲英语 3.6% / 北美 8.8% / 日韩 5.8% 都远低于）。所以华语反转的独特根源是**普通话自己塌陷**，不是方言突然变好。
- **关键数据**：跨国对照表（欧英 3.6% / 欧非主导 1.5% / 北美英 8.8% / 日韩 5.8% / 华普 24.4% / 华方 6.4%）。
- **可视化**：跨国烂片率条形图（按区域分主导/非主导分组，华语普通话置顶标红"异常值"）。
- **交互**：悬停看各组定义与样本量；点击"华语普通话"弹出边界说明。
- **视觉**：华语普通话柱体标红脉冲，其余灰蓝。
- **诚实提示**：类比只支撑"下限稳"共性，不支撑"均分反转"因果（弹窗/脚注注明）。
- **可选点睛（仅在能为叙事增彩时加入）**：华语普通话烂片率 24.4% 是全球异常值，但中国观众给出了全球最严苛评分（单片平均评价数 2.0 万，欧洲/北美对照倍数待重算）——这说明中国观众对电影有审美和追求，只是供给端没能匹配这份期待。（如果此点显得跑题或分散注意力，则不加。）
- **衔接钩子**：「全球共性说完了，但回到华语——方言片就一定是好片吗？」

### 5.6 第五幕 · 方言的真相：不是语言赢了，是创作态度赢了

**定位**：从"方言也有烂片"切入，经"同导演对比"论证，到"三波浪潮"展示好片——
        完成"破→立→展"三段递进，为终章升华铺路。

#### 第一步 · 破：方言片也有烂片（承接第四幕）

- **定位**：打破"方言=好片"的简单等式，承接第四幕"全球共性≠方言全好"。
- **故事**：方言片烂片率 6.4% 确实低于普通话 24.4%，但这不意味着方言片都是好片。
  方言也有低分片——天机·富春山居图(3.0)、西游记之大闹天宫(4.4)、澳门风云3(4.5)、
  长城(4.9)。这些片子的问题不在于"用了方言"，而在于创作路径本身——
  杂糅动作、特效堆砌、合家欢拼贴、工业化特效。
- **关键数据**：方言低分个案评分列表 + 低分原因标签（杂糅/堆砌/拼贴/工业化）。
- **可视化**：方言低分个案卡片（红色边框），与第三幕普通话低分个案形成呼应——
  说明低分的共性是创作路径问题，不是语言问题。
- **衔接**：「如果语言不是答案，那什么才是？」

#### 第二步 · 立：同导演对比——关键在于创作态度，不在语言

- **定位**：用同导演双栖对比证明"关键不是说了什么话，而是怎么讲故事"。
- **故事**：476 位同时拍过方言片和普通话片的导演中，70% 的方言片评分更高（平均分差 +0.64）。
  同一导演、同一系列、同一片名，换语言后评分差异巨大——
  说明质量差异来自项目本身的投入度和创作态度，而非导演能力的波动，
  更非语言本身的魔力。
- **关键数据**：群体证据（476 位，70%，+0.65）+ 4 位代表个案卡片（v4.4 口径待重算，旧基线示例值仅供结构参考）：
  - 刘镇伟：方言 vs 普通话（大话西游 vs 机器侠）
  - 袁和平：方言 vs 普通话（奇门遁甲新旧版对照）
  - 刘观伟：方言 vs 普通话（僵尸先生 vs 三笑）
  - 马伟豪：方言 vs 普通话（新扎师妹 vs 我们的十年）
- **可视化**：散点图（476 位导演的方言-普通话分差分布）+ 个案卡片（金色边框）。
- **交互**：点击散点图看点 → 弹出该导演作品列表。
- **观察**：港式爱情片在粤语语境里有生活质感，换普通话翻拍变成空壳。
  不是粤语"更好"，而是粤语语境下创作者更贴近生活、更认真投入。
- **行业现实三问题（折叠卡片·衔接段）**：读者自然会问——那普通话片为什么普遍创作态度差？
  三条供给侧解释（每张折叠卡片 1-2 句话，scale-tag 标注「行业观察/逻辑推论」）：
  - **① 院线排片机制**：流量逻辑下，"IP+流量明星"模式被反复复制，挤压了认真创作的空间。
    对应好故事维度：「被认真讲」。
  - **② 工业化流水线**：标准化制片追求"可复制的模板"，而非"有根的故事"。
    对应好故事维度：「人物真实」。
  - **③ 流量选角**：选角逻辑从"谁最适合角色"变成"谁自带最多粉丝"。
    对应好故事维度：「守住下限」。
  这三条恰好解释了普通话片烂片率 24.4%（2010s 高达 36.1%）的供给侧根源——
  行业现实在系统性地侵蚀好故事的生存空间。
  读者可自主展开了解，跳过不影响主线。
- **可视化**：三张折叠卡片（默认收起），点击展开详情，每张配 scale-tag 标签。
- **衔接**：「在这样的环境下，方言片反而成了'认真讲故事'的避风港——
  那么，认真讲出来的方言好故事，长什么样？」

#### 第三步 · 展：三波浪潮——好的方言片是什么样的

- **定位**：从"行业现实侵蚀好故事"→"方言片成为避风港"→"好的方言片长什么样"，顺理成章展示优秀作品。
- **故事**：三十年来，方言好片以三波浪潮涌现：
  - 第一波 港片粤语（无间道 9.3 / 南海十三郎 9.3 / 饮食男女 9.1）——
    在地生活质感，港式人情味；
  - 第二波 西南方言（路边野餐 / 地球最后的夜晚 / 无名之辈）——
    作者表达，边缘叙事，土地的力量；
  - 第三波 闽南语新浪潮（《给阿嬷的情书》）——
    家庭叙事，跨地域共鸣。
- **关键数据**：案例评分表（必须人工核验）；
  三波浪潮的时间分布、地域分布、类型分布。
- **可视化**：粒子时间轴聚类（按年代/浪潮聚团），三色浪潮带
  （港→蓝 / 西南→绿 / 闽南→橙），点击团看片单。
- **交互**：点击粒子 → 详情弹窗；"捞一部"随机按钮。
- **衔接钩子**：「这些好故事的共同点是什么？它们为什么能赢？」
  （自然引出终章）

### 5.7 终章 · 好故事赢了 + 文化多样性的土壤

**定位**：价值观收束——不是方言赢了普通话，是好故事赢了同质化；
        而文化多样性，是好故事生长的土壤。

**故事（三层递进，水到渠成）**：

  **层 1 · 好故事赢了同质化**
  回顾全篇逻辑链：方言烂片率低（6.4% vs 24.4%）→ 不是因为方言有魔力，
  而是选择方言的创作者往往更贴近生活、更认真投入（同导演对比 70% 方言更高）。
  好故事的敌人不是普通话，是同质化、流水线、IP+流量的模板化生产。
  方言只是恰好成为了一道"品质筛选机制"——
  愿意用方言讲故事的创作者，往往本来就在认真对待创作。

  **层 2 · 好故事的标准（数据支点·好故事五维度框架）**
  那么好故事的标准是什么？从数据中归纳五个维度：
  - ① 有根（Rootedness）：故事从真实的人、地、时中生长出来。
    数据代理：类型均分线索——方言片内动画(7.62)、纪录片(7.57)、历史(7.53)、
    家庭(7.42) 均分较高，共同点是"有真实原型"或"有具体根基"；
    恐怖(5.91)、悬疑(6.14)等类型均分偏低。
  - ② 被认真讲（Craft）：创作者有自觉追求。
    数据代理：同导演双栖差异（70% 方言更高，平均分差 +0.64）。
  - ③ 人物真实（Character Truth）：人有血有肉有矛盾。
    数据代理：方言片要求演员掌握特定语言/口音，本身就是"进入角色"。
  - ④ 能打动人（Resonance）：触及基本人类经验。
    数据代理：方言高分片（≥8 分）评价人数中位 17,707 vs 普通话 1,278，差约 14 倍。
  - ⑤ 守住下限（Floor）：不敷衍、不模板化。
    数据代理：烂片率方言 6.4% vs 普通话 24.4%。
  决定好坏的不是"讲什么"，而是"怎么讲"。

  **层 3 · 什么造就了好故事？——文化多样性的土壤**
  好故事从哪来？不是凭空产生的——它需要土壤。
  中国电影的语言字段记录了数百种不同语言/方言标签（精确计数待重算落盘）。
  方言电影里除粤语(6.51)外，上海话(7.44)、闽南语(7.39)、藏语(7.20)、
  四川话(7.06) 等小方言均分都更高——
  质量不靠一地方言独大，而是百花齐放。
  
  是什么让中国有这么多好故事可讲？
  是这片土地——辽阔的地域、上百种语言、几千年的历史、
  56 个民族的风土人情、从高原到海岸的地理跨度。
  文化多样性不是结论，是土壤——
  它为"有根的故事"提供了取之不尽的素材，
  让每一个"认真讲"的创作者都有矿可挖。
  
  所以这不是方言对普通话的胜利，
  而是好故事赢了同质化，多样性滋养了好故事。

**回响 · 回到开头的问题**（《给阿嬷的情书》第三次出现——价值升华锚点）：

  回到开头那个问题：一部闽南语（潮汕话）家庭电影，凭什么让全国观众共鸣？

  五幕数据已经给出了答案——
  不是因为它说了闽南语，也不是因为它说了普通话，
  而是因为它讲了一个**有根的、被认真讲的、关于真实的人的故事**。

  《给阿嬷的情书》已在我们的数据集里，
  并计入方言片——它曾因交付数据缺语言列被误归普通话组，对照豆瓣实页修正补收：
  数据会出错，但数据也能自我纠正，这也是本作品诚实的一部分；
  而数据集里 63,025 部电影的规律指向同一个结论：
  方言烂片率 6.4% vs 普通话 24.4%——
  不是因为方言有魔力，而是选择方言的创作者，
  往往本来就在认真对待创作。

  方言不是答案。好故事才是。

  而好故事的土壤——这片土地上的数百种语言/方言标签、
  上千种方言、几千年的记忆、从高原到海岸的地理跨度——
  我们从不缺。

  **收尾金句**：
  「我们的土地孕育了上百种语言、无数故事，本就是创作的富矿。
  今天我们在平均分上仍落后欧美（方言 6.62 < 欧洲 7.17、北美 6.75），
  但那道鸿沟是质量的鸿沟，不是文化的鸿沟。
  把质量做好，追上乃至并肩，并非奢望——
  因为好故事的土壤，我们从不缺。」

  **设计意图**：
  - "回响"段落将《给阿嬷的情书》从封面的**具体电影**升维为**价值象征**——
    它不再是一部电影，而是"好故事赢了同质化"这个论点的化身。
  - 读者在终章回想封面的《阿嬷》时，会有"原来如此"的认知升级感：
    封面是"问号"，终章是"叹号"。
  - 形成完整的"提问→论证→回答"叙事闭环，
    让评委感受到结构上的精心设计而非随意拼凑。

**关键数据**：
- 回顾：3,045 vs 9,813；6.4% vs 24.4%
- 好故事五维度框架 + 类型均分线索
- 多样性：数百种语言标签（精确计数待重算）/ 多方言均分高于粤语
- 格局：方言 6.62 < 欧洲 7.17、北美 6.75

**可视化**：
- 类型均分条形图（金/灰/红三色区分高/中/低均分类型）
- 方言各语言组均分条形（呼应"非粤语独大"）
- 四组对比图（诚实展示仍落后欧美）
- 粒子收束：从星云回归，方言粒子逐一点亮，
  最终汇聚——呼应封面粒子星云的"散→聚"弧线，形成视觉闭环

**交互**：进入探索舱按钮 + 分享提示；点击类型均分条形图可查看该类型 Top 片单。

**落点句**：「靠质量去赢——这，才是真正的文化自信。」

**UI/UX 设计规格**：

**粒子收束三阶段动效**

| 阶段 | 时间段 | 粒子行为 | 缓动函数 |
|------|--------|---------|----------|
| ① 离散 | 0–1.5s | 粒子保持漂浮，逐渐减速至静止（速度 `1.0→0.0`） | `ease-out-cubic` |
| ② 聚拢 | 1.5–4s | 方言粒子（琥珀色）沿螺旋路径向画面中心缓动汇聚；普通话粒子保持背景层缓慢漂浮，不参与收束 | `ease-in-out-cubic` |
| ③ 点亮 | 4–5.5s | 汇聚完成后，从中心向外逐个脉冲闪烁（每粒子延迟 120ms，闪烁持续 300ms） | `ease-out-quint` |

**实现方式**：使用 `requestAnimationFrame` 逐帧插值粒子位置，螺旋路径公式：
```
r(t) = R * (1 - t)  // 半径递减
θ(t) = θ₀ + 2π * t * 2  // 角度递增（2 圈）
```
其中 `t` 为归一化时间（0→1），`R` 为初始距离，`θ₀` 为初始角度。

**收束与文案时序配合**

| 时间点 | 粒子状态 | 文案状态 |
|--------|---------|----------|
| 0s | 开始减速 | 层 1「好故事赢了同质化」淡入 |
| 1.5s | 开始聚拢 | 层 1 文案完全显示 |
| 4s | 聚拢完成，开始点亮 | 层 2「好故事五维度」从底部滑入 |
| 5.5s | 点亮完成 | 层 3「文化多样性的土壤」逐行显现（每行 300ms 间隔） |
| 6.5s | 粒子保持静止微光 | 回响卡片（《给阿嬷的情书》回归）从中心展开 |
| 8s | 粒子开始缓慢漂浮 | 收尾金句独占视口显示 |

**长文案排版布局**

```css
/* 三层递进文案：居中窄栏 */
.finale-narrative {
  max-width: 640px;
  margin: 0 auto;
  padding: 0 var(--space-md);
}

/* 每层之间留白 */
.finale-layer {
  min-height: 60vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

/* 层标题 */
.finale-layer-title {
  font-family: var(--font-serif);
  font-size: 1.6rem;
  color: var(--color-amber);
  margin-bottom: var(--space-lg);
}

/* 正文 */
.finale-layer p {
  font-size: 1.05rem;
  color: var(--color-text-mid);
  line-height: 1.8;
}

/* 关键数据高亮 */
.finale-layer strong {
  color: var(--color-amber);
  font-weight: 700;
}
```

**回响卡片样式**

```css
/* 与引言 5.0b 毛玻璃卡呼应，但边框改为琥珀色 */
.finale-echo-card {
  backdrop-filter: blur(16px) saturate(1.4);
  background: rgba(13, 13, 13, 0.55);
  border: 1px solid rgba(212, 165, 116, 0.3);
  border-radius: var(--radius-lg);
  max-width: 560px;
  margin: 0 auto;
  padding: 32px 36px;
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.4);
  /* 入场动效 */
  opacity: 0;
  transform: scale(0.95);
  transition: opacity 800ms var(--ease-out-quint),
              transform 800ms var(--ease-out-quint);
}

.finale-echo-card.visible {
  opacity: 1;
  transform: scale(1);
}
```

**收尾金句视觉规格**

```css
/* 独占一个视口高度 */
.finale-closing {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: var(--space-xl);
}

.finale-closing-quote {
  font-family: var(--font-serif);
  font-size: 1.4rem;
  line-height: 1.8;
  max-width: 600px;
  /* 琥珀色渐变文字 */
  background: linear-gradient(90deg, #d4a574, #e8c9a0);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

**情绪渲染音效（可选）**

| 参数 | 值 | 备注 |
|------|-----|------|
| 触发时机 | 粒子收束阶段（1.5–5.5s） | 与视觉动效同步 |
| 音效类型 | 弦乐持续音（ambient drone） | 低沉、温暖、不抢戏 |
| 音量 | `0.1–0.15`（相对最大音量） | 极低音量，仅作氛围渲染 |
| 渐入 | 0→0.15 在 1.5s 内 | 与粒子聚拢同步 |
| 渐出 | 0.15→0 在 2s 内 | 点亮完成后淡出 |
| 用户控制 | 尊重 `prefers-reduced-motion`；提供静音按钮 | 无障碍要求 |
| 实现方式 | Web Audio API + `AudioBuffer` | 预加载，避免延迟 |

### 5.8 探索舱（四卷，方言核心重构）

- **卷一 · 年代弧线**：方言 vs 普通话分年代均分/烂片率/高分率，逐年交叉滑块（复用第一幕深化）。
- **卷二 · 地区/语言对照**：华语 vs 欧洲(英/欧语) vs 北美 vs 日韩 的语言分层烂片率（复用第四幕深化，可下钻样本）。
- **卷三 · 语言构成**：粤语 / 西南官话 / 吴语 / 闽南语等方言细分 + 非粤语优势展示；混杂类型剔除演示。
- **卷四 · 交叉筛选**：类型 × 年代 × 语言(方言/普通话/外部) 自由组合筛选，实时出分布与 Top 片单。
- **交互**：四卷标签页切换；图表联动筛选；每卷"回到首页叙事"返回键。

**UI 排版规格**：

**整体布局**

```
┌──────────────────────────────────────────────┐
│  标签栏：年代弧线 | 地区对照 | 语言构成 | 交叉筛选  │  ← 60px 高
├──────────────────────────────────────────────┤
│                                              │
│              主图表区（60% 高度）              │
│                                              │
├──────────────────────────────────────────────┤
│  筛选摘要条：「粤语 · 2010s · 剧情片 | 共 47 部」 │  ← 40px 高
├──────────────────────────────────────────────┤
│                                              │
│        底部数据面板（40% 高度）              │
│        （insight 文本 + 筛选控件）              │
│                                              │
└──────────────────────────────────────────────┘
```

**标签栏样式与切换动效**

```css
.explore-tabs {
  display: flex;
  height: 60px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.explore-tab {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-sans);
  font-size: 0.9rem;
  color: var(--color-text-low);
  border-bottom: 2px solid transparent;
  transition: all var(--duration-normal) var(--ease-out-quint);
}

.explore-tab:hover {
  color: var(--color-text-mid);
}

.explore-tab.active {
  color: var(--color-amber);
  font-weight: 700;
  border-bottom-color: var(--color-amber);
}
```

**切换动效**：内容区 `opacity: 1→0`（200ms）→ 切换数据 → `opacity: 0→1`（400ms）

**卷四交叉筛选布局**

```
┌─────────────────┬─────────────────────────┐
│  筛选器区（30%） │    图表区（70%）        │
│                 │                         │
│  类型：[下拉]    │    [ECharts 图表]        │
│  年代：[滑块]    │                         │
│  语言：[多选标签] │                         │
│                 │                         │
│  [重置筛选]     │                         │
└─────────────────┴─────────────────────────┘
```

**联动筛选反馈**

| 元素 | 筛选激活时 | 筛选未激活时 |
|------|----------|------------|
| 被选中粒子/柱体 | `opacity: 1` | — |
| 未选中粒子/柱体 | — | `opacity: 0.15` |
| 筛选摘要条 | 显示当前筛选条件（如「粤语 · 2010s · 剧情片 | 共 47 部 | 均分 7.12」） | 隐藏 |
| 重置按钮 | 可见，点击后清除所有筛选 | 隐藏 |

**筛选摘要条样式**

```css
.filter-summary {
  height: 40px;
  background: var(--color-surface-2);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  display: flex;
  align-items: center;
  padding: 0 var(--space-md);
  font-family: var(--font-sans);
  font-size: 0.85rem;
  color: var(--color-text-mid);
}

.filter-summary strong {
  color: var(--color-amber);
  margin: 0 var(--space-xs);
}
```

**“回到首页叙事”按钮**

```css
.back-to-narrative {
  position: fixed;
  right: var(--space-lg);
  bottom: var(--space-lg);
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: var(--color-surface-2);
  border: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all var(--duration-normal) var(--ease-out-quint);
  z-index: var(--z-nav);
}

.back-to-narrative:hover {
  background: var(--color-amber);
  border-color: var(--color-amber);
}

/* 点击后平滑滚动至首页对应幕的锚点 */
/* scrollIntoView({ behavior: 'smooth', block: 'start' }) */
```

**空状态设计**

筛选无结果时，图表区显示：
- 图标：简笔线条风格的空放大镜（SVG，120x120px，灰色 `#888`）
- 主文案：「当前筛选条件下没有匹配的电影」（1.1rem，居中）
- 副文案：「调整筛选条件试试」（0.9rem，灰色）
- 按钮：「重置筛选」（琥珀色边框，点击后清除所有筛选）

### 5.9 方法论 / 数据边界页

- **定位**：建立评委信任的"诚实页"。
- **内容**：
  - 数据来源与口径说明（全局口径 vs 中国区域口径差异）；
  - **叙事引子诚实边界**（scale-tag 标注「数据可证」）：
    > *《给阿嬷的情书》（2026）已在本作品数据集内，并计入方言片（潮汕话居首，Tier 2a）；
    > 原始交付数据缺语言列，曾被默认回填为“汉语普通话”而误归普通话组，
    > 2026-08-19 对照豆瓣实页修正补收；
    > 作为交付元数据缺漏与数据溯源修复的案例呈现，正文不引用其具体评分。
    > 下文所有数据均来自 63,025 部电影的实际核算。*
    
    与方法论页诚实清单形成双重锚点。
  - **scale-tag 行内标注系统**（三种标签区分证据强度）：
    - <span class="scale-tag data">数据可证</span>：数据直接证明（如均分、烂片率、评价人数）
    - <span class="scale-tag infer">推测性口吻</span>：数据相关但无法证明因果（如"方言是隔离带"解读）
    - <span class="scale-tag logic">行业观察/逻辑推论</span>：数据集无对应字段（如演员适配度、院线排片机制）
  - 9 条诚实清单（演唱会混杂、合并桶、英文片误标、评价≠票房、无票房字段、近期数据区域构成偏倚、门槛效应为解读、外部参照边界、引子影片语言列缺漏与修正补收）；
  - 外部参照三层方法说明；
  - 票房缺失的外部取证建议；
  - 行业现实三问题（院线排片/工业化流水线/流量选角）已在第五幕第二步以折叠卡片形式呈现；方法论页仅作补充说明，标注为"待探索方向"，链接回第五幕对应段落。
- **交互**：锚点目录；可折叠的"争议点"卡片；点击 scale-tag 弹出详细说明。

**UI 排版规格**：

**布局**

```css
/* 纯阅读页，单栏居中 */
.methodology-content {
  max-width: 720px;
  margin: 0 auto;
  padding: var(--space-2xl) var(--space-md);
}

/* 左侧固定锚点目录（桌面端） */
.methodology-toc {
  position: fixed;
  left: var(--space-lg);
  top: 50%;
  transform: translateY(-50%);
  width: 200px;
  font-family: var(--font-sans);
  font-size: 0.85rem;
}

/* 移动端：顶部折叠目录 */
@media (max-width: 768px) {
  .methodology-toc {
    position: sticky;
    top: 0;
    left: 0;
    transform: none;
    width: 100%;
    background: var(--color-surface-1);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: var(--space-md);
  }
}
```

**scale-tag 行内标注样式**

| 标签类型 | 背景色 | 字色 | 圆角 | 内边距 | 字号 |
|---------|--------|------|------|--------|------|
| `data`（数据可证） | `rgba(84, 112, 198, 0.15)` | `#5470C6` | `4px` | `2px 8px` | `0.75rem` |
| `infer`（推测性口吻） | `rgba(212, 165, 116, 0.15)` | `#b8874a` | `4px` | `2px 8px` | `0.75rem` |
| `logic`（行业观察/逻辑推论） | `rgba(255, 255, 255, 0.08)` | `#888888` | `4px` | `2px 8px` | `0.75rem` |

```css
.scale-tag {
  display: inline-block;
  border-radius: var(--radius-sm);
  padding: 2px 8px;
  font-size: 0.75rem;
  font-family: var(--font-sans);
  font-weight: 500;
  margin-left: 4px;
  cursor: pointer;
  transition: opacity var(--duration-fast) ease;
}

.scale-tag:hover {
  opacity: 0.8;
}

.scale-tag.data {
  background: rgba(84, 112, 198, 0.15);
  color: #5470C6;
}

.scale-tag.infer {
  background: rgba(212, 165, 116, 0.15);
  color: #b8874a;
}

.scale-tag.logic {
  background: rgba(255, 255, 255, 0.08);
  color: #888888;
}
```

**scale-tag Popover 规格**

```css
.scale-tag-popover {
  position: absolute;
  max-width: 280px;
  background: var(--color-surface-2);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  box-shadow: var(--shadow-tooltip);
  font-size: 0.85rem;
  line-height: 1.6;
  z-index: var(--z-toast);
  /* 入场动效 */
  opacity: 0;
  transform: translateY(4px);
  transition: opacity var(--duration-fast) ease,
              transform var(--duration-fast) ease;
}

.scale-tag-popover.visible {
  opacity: 1;
  transform: translateY(0);
}
```

**诚实清单手风琴卡片**

```css
.honesty-accordion {
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-md);
  margin-bottom: var(--space-sm);
  overflow: hidden;
}

.honesty-header {
  display: flex;
  align-items: center;
  padding: var(--space-md);
  cursor: pointer;
  background: var(--color-surface-1);
  transition: background var(--duration-fast) ease;
}

.honesty-header:hover {
  background: var(--color-surface-2);
}

.honesty-header::before {
  content: '▶';
  margin-right: var(--space-sm);
  transition: transform var(--duration-normal) var(--ease-out-quint);
}

.honesty-accordion.open .honesty-header::before {
  transform: rotate(90deg);
}

.honesty-body {
  max-height: 0;
  overflow: hidden;
  transition: max-height var(--duration-normal) var(--ease-out-quint);
}

.honesty-accordion.open .honesty-body {
  max-height: 500px; /* 足够容纳内容 */
}

.honesty-body-inner {
  padding: var(--space-md);
  font-size: 0.9rem;
  line-height: 1.7;
  color: var(--color-text-mid);
}
```

**锚点跳转视觉反馈**

```css
/* 跳转目标项短暂高亮 */
.methodology-section {
  scroll-margin-top: 80px; /* 避开固定目录遮挡 */
  transition: background-color 1.5s ease;
}

.methodology-section.highlight {
  background-color: rgba(212, 165, 116, 0.08);
}

/* 1.5s 后淡出 */
/* JS: setTimeout(() => el.classList.remove('highlight'), 1500) */
```

**争议点卡片**

```css
.controversy-card {
  border-left: 3px solid var(--color-danger);
  background: var(--color-surface-2);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  margin-bottom: var(--space-md);
}

.controversy-header {
  display: flex;
  align-items: center;
  cursor: pointer;
}

.controversy-header::before {
  content: '⚠ 争议';
  font-size: 0.75rem;
  color: var(--color-danger);
  margin-right: var(--space-sm);
  font-weight: 700;
}

.controversy-body {
  max-height: 0;
  overflow: hidden;
  transition: max-height var(--duration-normal) ease-out,
              opacity var(--duration-normal) ease-out;
  opacity: 0;
}

.controversy-card.open .controversy-body {
  max-height: 300px;
  opacity: 1;
}
```

---

## 6. 交互逻辑设计规范（统一标准）

| 交互   | 规范                                              |
| ---- | ----------------------------------------------- |
| 滚动驱动 | IntersectionObserver 触发幕切换；粒子随叙事维度重排（500ms 过渡）  |
| 比较控件 | **常显、默认展开**（修复现有 `opacity:0.16` 折叠问题）；每幕仅一个核心控件 |
| 悬停   | 粒子 → 浮层（片名+评分+年份）；图表 → tooltip                  |
| 点击   | 粒子/案例 → 详情弹窗（`core.js` 复用）；导航项 → 锚点平滑滚动         |
| 筛选   | 探索舱四卷联动；支持"重置"                                  |
| 随机打捞 | "捞一部"按钮随机高亮一部方言片并弹详情                            |
| 响应式  | 移动端：粒子降密度、图表转纵向、控件常显、字号适配（**新增**）               |
| 性能   | 粒子数 > 1 万 时降采样；ECharts `lazyUpdate`；首屏骨架屏（修复白屏） |
| **scale-tag 行内标注** | 三种标签：<span class="scale-tag data">数据可证</span> / <span class="scale-tag infer">推测性口吻</span> / <span class="scale-tag logic">行业观察/逻辑推论</span>；点击标签弹出详细说明；正文关键数据点后强制标注 |

### 6.1 幕间过渡动效规格

**粒子场景切换**

| 阶段 | 时长 | 旧场景行为 | 新场景行为 |
|------|------|----------|----------|
| ① 淡出 | 300ms | 粒子 `opacity: 1→0.1` + 向画面边缘扩散（速度 `0.5→2.0`） | — |
| ② 聚合 | 500ms | — | 新场景粒子从画面中心以 `ease-in-out-cubic` 聚合到目标位置 |

**实现方式**：使用 `requestAnimationFrame` 保证 60fps，粒子位置插值公式：
```js
// 旧场景扩散
particle.x += (particle.x - centerX) * 0.02;
particle.y += (particle.y - centerY) * 0.02;
particle.opacity = Math.max(0.1, particle.opacity - 0.02);

// 新场景聚合
particle.x = lerp(particle.x, targetX, easeInOutCubic(t));
particle.y = lerp(particle.y, targetY, easeInOutCubic(t));
```

**文案层切换**

| 阶段 | 时长 | 旧幕文案 | 新幕文案 |
|------|------|---------|----------|
| ① 滑出 | 400ms | `translateY: 0→-60px` + `opacity: 1→0` | — |
| ② 滑入 | 500ms | — | `translateY: 60px→0` + `opacity: 0→1`（delay 200ms） |

**引导线系统过渡**

| 阶段 | 时长 | 行为 |
|------|------|------|
| ① 旧线淡出 | 200ms | 旧引导线 `opacity: 1→0` |
| ② 新线绘制 | 400ms | 新引导线从 0 长度绘制到目标长度（使用 `stroke-dashoffset` 动画） |

### 6.2 侧边章节目录规格

**默认状态**

```css
.side-nav {
  position: fixed;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 48px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  z-index: var(--z-nav);
}

.side-nav-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--color-text-low);
  transition: all var(--duration-normal) var(--ease-out-quint);
  cursor: pointer;
}

.side-nav-dot.active {
  width: 12px;
  height: 12px;
  background: var(--color-amber);
}
```

**Hover/展开状态**

```css
.side-nav:hover {
  width: 200px;
  transition: width var(--duration-normal) var(--ease-out-quint);
}

.side-nav-label {
  opacity: 0;
  margin-left: var(--space-sm);
  font-family: var(--font-sans);
  font-size: 0.85rem;
  color: var(--color-text-mid);
  white-space: nowrap;
  transition: opacity var(--duration-normal) ease;
}

.side-nav:hover .side-nav-label {
  opacity: 1;
}
```

**进度指示线**

```css
.side-nav-progress {
  width: 2px;
  background: var(--color-amber);
  margin-top: 4px;
  transition: height var(--duration-normal) var(--ease-out-quint);
  /* 高度随当前幕内滚动进度增长（0%→100%） */
  /* JS: height = scrollProgress * maxLineHeight */
}
```

**点击交互**

```js
// 点击圆点平滑滚动至对应幕
dot.addEventListener('click', () => {
  const section = document.getElementById(sectionId);
  section.scrollIntoView({ behavior: 'smooth', block: 'start' });
});
```

**移动端降级**

```css
/* 隐藏侧边目录，改为顶部进度条 */
@media (max-width: 768px) {
  .side-nav {
    display: none;
  }
  
  .top-progress-bar {
    position: fixed;
    top: 0;
    left: 0;
    height: 3px;
    background: var(--color-amber);
    width: 0%; /* 随全局滚动进度增长 */
    z-index: var(--z-nav);
    transition: width var(--duration-fast) ease;
  }
}
```

---

## 7. 用户体验流程（用户旅程）

| 阶段   | 用户目标       | 行为      | 设计支撑        |
| ---- | ---------- | ------- | ----------- |
| 1a 吸引 | 被视觉氛围和标题抓住，产生"方言电影为什么赢了？"的好奇 | 看封面 | 粒子星云 + 主标题 + 副标题 + KPI 卡片 |
| 1b 共情 | 被《阿嬷》故事打动，从好奇进入"凭什么？"的追问 | 读引言 | 叙事引子 + 诚实边界声明 + 引言钩子 |
| 2 发现 | 在序章数据交互中看到烂片率反差，意外发现高分率纠偏，用年代按钮察觉差距随年代变化 | 操作 scene-lab | 粒子散点图 + 引导线 + 高分率数据卡 + 年代按钮 + 叙事回声标记 |
| 3 追问 | "两个年代竟然相反？让我看完整的线" | 拖年份滑块看逐年弧线 | 第一幕逆转弧线——从"两个点"展开为"完整的线" |
| 4 拆解 | 理解为何反转     | 切口径/看塌陷 | 第二、三幕比较控件   |
| 5 格局 | 放到全球看      | 看跨国条形   | 第四幕拔高       |
| 6 反思 | "方言=好片？" | 看方言低分个案 | 第五幕第一步 |
| 7 顿悟 | "不是语言，是态度" | 看同导演对比 | 第五幕第二步 |
| 8 共鸣 | "好的方言片长什么样" | 点粒子/捞一部 | 第五幕第三步 |
| 9 升华 | "好故事+多样性土壤"；回想封面《阿嬷》→"原来如此" | 读终章回响 | 终章三层递进 + 回响闭环（《阿嬷》第三次出现） |
| 10 探索 | 自己验证       | 进探索舱    | 四卷深潜        |
| 11 信任 | 确认严谨       | 读方法论    | 诚实清单        |

---

## 8. 数据层新增/改造清单（开发执行用）

在 `config.py` / `rebuild.py` 新增：

1. `dialect_vs_mandarin_by_decade`（分年代均分/烂片率/高分率/评价数）；
2. `type_controlled`（原始 / 剔除演唱会·纪录 / 仅剧情片 三口径）；
3. `cantonese_vs_non`（粤语 vs 非粤语方言）；
4. `global_lang_layers`（欧洲英/欧语、日韩、北美英、华语主导/非主导）；
5. `yearly_cross_points`（2000–2015 逐年差）；
6. `wave_cases`（三波浪潮精选片单，含人工核验标记）；
7. `lang_diversity`（方言各语言组：影片数/均分/烂片率，按"语言"多标签拆分；支撑终章多样性地图）；
8. `group_vs_west`（方言/中国非方言/欧洲/北美 的均值/烂片率/8+占比 + 按年代均值；支撑终章四组对比图 `compare_dialect_west.html`）；
9. **`dual_director_comparison`**（同导演双栖对比：476 位导演的方言-普通话评分差分布 + 代表个案卡片数据；支撑第五幕 Part B）；
10. **`genre_avg_rating`**（类型均分统计：戏曲/纪录片/传记/剧情/历史/奇幻/悬疑等类型的均分分布；支撑终章"好故事五维度"中的类型线索）。

### 8.1 数据载荷契约（定稿）

**总则**：前端正文数字禁止手工硬编码，一律引用数据载荷；载荷由确定性脚本从冻结基线（derived_movies.csv，指纹 c64962992e）生成；口径定义继承 `scripts/sync_preview_dialect_v43_20260819.py` 文档头（below5=烂片率、high8=≥8 分占比、yearly 双方 n≥5、diversity min n≥10、genreAvg min n≥30 等），**2026-08-19 已固化进 `scripts/data_aggregator.py` 的 `build_dialect_aggregates`，正式载荷为 `data/frontend/dialect_aggregates.json`（五件套之一，指纹与 manifest 一致，测试 `DialectAggregatesTests` 锁定关键值）**。`frontend/preview_dialect.html` 已移至 `archive/preview_dialect.html`，内嵌 AGG 仅作历史证据，不再作为取值来源（其中 drama 普通话均分 6.25、diversity 第 10 位粤语两处为当年转录错误，权威值以载荷为准：6.24 / 武汉话 6.6）。

| # | 载荷块名 | 字段 | 生成脚本 | 消费幕位 |
| --- | --- | --- | --- | --- |
| 1 | `dialect_vs_mandarin_by_decade`（载荷键 `by_decade`） | 分年代 n/mean/below5/high8/delta | data_aggregator.py（已交付 `data/frontend/dialect_aggregates.json`） | 序章/第一幕/卷一 |
| 2 | `type_controlled` | raw/exclude/drama 三口径 mean+below5（6.62/6.11、6.58/5.93、6.86/6.24） | 同上（已交付） | 第三幕/卷三 |
| 3 | `cantonese_vs_non` | 粤语 6.51 vs 非粤语 7.22（n/below5） | 同上（已交付） | 第三幕/卷三 |
| 4 | `global_lang_layers`（载荷键 `global_layers`） | 六层烂片率（欧非主导 1.5/欧英 3.6/日韩 5.8/华方 6.4/北美英 8.8/华普 24.4，含 n） | 同上（已交付） | 第四幕/卷二 |
| 5 | `yearly_cross_points`（载荷键 `yearly`） | 1990–2020 逐年 delta（双方 n≥5） | 同上（已交付） | 第一幕滑块/卷一 |
| 6 | `wave_cases` | 三波浪潮精选片单（港/西南/闽南 15 部，`WAVE_CASE_IDS` 人工策展常量） | 同上（已交付）+ 人工核验 | 第五幕第三步 |
| 7 | `lang_diversity` | 方言语言标签 mean/n（min n≥10，top10 客家话 7.88→武汉话 6.6） | 同上（已交付） | 终章层 3/卷三 |
| 8 | `group_vs_west` | 方言/中国非方言/欧洲/北美 均值/烂片率/8+占比 + 分年代均值 | 未交付，留待后续数据层迭代 | 终章四组对比 |
| 9 | `dual_director_comparison`（载荷键 `dual_director`） | 476 位双栖导演分差直方图 + 70%/+0.65 群体指标 + 代表个案 | 同上（已交付） | 第五幕第二步 |
| 10 | `genre_avg_rating`（载荷键 `genre_avg`） | 方言片类型均分（n≥30，动画 7.62 居首，含 top3 片单） | 同上（已交付） | 终章层 2 |
| — | `narrative_facts.json` | mandarin_dialect 块（China 口径 9,813/3,045、分年代、+0.51）/ languages 块（全量 3,338） | scripts/extract_narrative.py | 序章数据卡/高分率纠偏 |
| — | `frontend_dataset.json` 头部 | 总行数 63,025 / China 12,858 / 年份范围 / 指纹 | scripts/data_aggregator.py | 封面 KPI/方法论页 |

**按幕优先级排序**（序章/第一幕/第三幕所需优先）：narrative_facts mandarin_dialect → ① by_decade → ⑤ yearly_cross_points → ② type_controlled → ④ global_lang_layers → ⑨ dual_director_comparison → ⑦ lang_diversity / ⑩ genre_avg_rating → ⑥ wave_cases / ⑧ group_vs_west / ③ cantonese_vs_non。

**待重算清单**（本定稿中无法从现行权威载荷直接取值）：① 导演个案卡片四位导演 v4.4 分差值；② 语言标签精确计数（实测拆分标签 672 / 原始字段 2,960，需与正文口径对齐后落盘）；③ 欧洲/北美单片平均评价数对照倍数。列入数据层任务，落地后回填正文。

---

## 9. 开发执行优先级（里程碑，裁剪范围已定稿）

> **裁剪定稿（2026-08-19）**：探索舱首批交付卷一（年代弧线）+ 卷四（交叉筛选），卷二/卷三视余量；移动端适配做到 mobile 档（768px），mobile-sm 仅保底不崩；分享卡片与音效降为可选（P3+）；数据层十项聚合按 §8.1 幕优先级排序落地。硬截止 2026-08-30 公网提交，一切范围决策服从该时间约束。

- **P0（必做，决定评委能否"看到交互"）**：KPI 卡片（动态加载）+ 修复控件常显 + 新增侧边目录/进度 + 首屏骨架屏 + 移动端适配（768px 档）；部署公网拿投稿 URL。
- **P1（核心叙事）**：封面钩子 + 序章（含高分率纠偏）+ 第一~五幕（含同导演对比、个案对比卡片）+ 终章（含好故事五维度）文案与可视化落地（复用粒子/图表引擎）；scale-tag 行内标注系统全局应用；数据层按 §8.1 优先级产出序章/第一幕/第三幕所需聚合。
- **P2（深度+信任）**：探索舱卷一 + 卷四首批交付 + 方法论边界页（含 scale-tag 说明 + 行业现实精简版折叠卡片）+ 数据层剩余聚合。
- **P3（打磨，视余量）**：卷二/卷三、随机打捞、案例人工核验回填、视觉统一、性能优化、类型均分条形图。
- **P3+（可选，不阻塞交付）**：分享卡片生成、情绪渲染音效、mobile-sm 精修。

---

## 10. 与现有"滤镜版"代码的衔接

- **复用**：`core.js`（数据服务+弹窗）、粒子引擎、ECharts 组件、`serve.py`、数据流水线框架。
- **重构**：`index.html` 八幕文案与结构改为方言五幕主线；`app.js` 场景控制重写；`vol1-4` 重构为方言四卷；新增 `scenes/`、`explore/` 模块化拆分。
- **删除/归档**：旧的"滤镜/欧洲偏见"叙事文案与对应幕（保留其稳健性检验方法论，融入第四幕外部参照）。

---

## 11. 风险与边界（呼应诚实清单）

- 方言 Top 混杂演唱会/纪录片 → 正文同显原始与剔除后均分（加分项）；
- `Is_Dialect` 为合并桶，无法证伪具体方言崛起 → 方法论注明；
- 语言字段为多标签（如"粤语 / 汉语普通话"），方言细分样本量偏小（闽南语 154、上海话 64、四川话 35、藏语 24 部）→ 多样性地图仅作趋势呈现，绝对数谨慎引用；
- 英文片误标方言 → 案例人工核验；
- 评价人数 ≠ 票房 → 仅作关注度代理；
- 数据集无票房/成本 → Q3 须外部取证或标注"待补充"；
- 近期数据区域构成偏倚（数据集覆盖至 2026 年，2022 年后共 7,190 部，以北美/欧洲/东亚为主、中国大陆占 16%；2020s 中国方言片仅 57 部）→ 时间偏倚注明；
- **引子影片语言列缺漏与修正补收**：《给阿嬷的情书》（2026）的交付数据（delivery_20260817）无语言列，曾被默认回填“汉语普通话”误归普通话组；2026-08-19 对照豆瓣实页（潮汕话居首）修正补收，基线更新为 v4.4（3,045 / 9,813）→ 在引言与方法论页双重披露；
- 外部参照类比只支撑"下限稳"共性，不支撑"均分反转"因果；
- **同导演双栖对比个案样本量有限**（如袁和平仅 2 部普通话片），群体证据（476 位，70% 方言更高）统计效力更强，但个体案例需注明"不能代表全部导演"；
- **词频分析覆盖率不均**（Gemini 评价方言 62.4% vs 普通话 46.5%），"模板化"解读为主观观察，非客观标签；
- **"好故事五维度"框架为归纳总结**，数据代理仅为旁证支撑，非因果证明；
- **行业现实三问题**（院线排片/工业化流水线/流量选角）无数据直接支撑，仅作逻辑推论，已在第五幕第二步以折叠卡片形式呈现（每张配 scale-tag 标注「行业观察/逻辑推论」），方法论页仅作补充说明。

---

## 12. 全局状态与异常处理

### 12.1 骨架屏（Loading Skeleton）

**视觉结构**

```
┌─────────────────────────────────────────────┐
│  ████████████████████████████  ← 标题脉冲条  │
│  ██████████████████████        ← 副标题脉冲条  │
│                                             │
│     ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌    ← 粒子占位圆点  │
│   ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌   （静态，灰色）  │
│     ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌ ◌              │
│                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │ KPI │ │ KPI │ │ KPI │ │ KPI │ ← 卡片骨架  │
│  └─────┘ └─────┘ └─────┘ └─────┘          │
│                                             │
│           ↓ 向下阅读 ↓          ← 滚动提示骨架  │
└─────────────────────────────────────────────┘
```

**实现方式**

在 `index.html` 的 `<main>` 内预设静态 HTML 骨架，添加 `.skeleton` CSS 类；数据加载完成后由 JS 移除 `.skeleton` 并淡入真实内容。

```css
/* 骨架脉冲动画 */
.skeleton-shimmer {
  background: linear-gradient(
    90deg,
    #1a1a1e 25%,
    #252528 50%,
    #1a1a1e 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.8s infinite;
  border-radius: var(--radius-md);
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* 标题骨架条 */
.skeleton-title {
  height: 32px;
  width: 70%;
  margin-bottom: var(--space-md);
}

/* 副标题骨架条 */
.skeleton-subtitle {
  height: 20px;
  width: 50%;
  margin-bottom: var(--space-xl);
}

/* 粒子占位圆点 */
.skeleton-particles {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.skeleton-dot {
  position: absolute;
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.08);
}

/* KPI 卡片骨架 */
.skeleton-kpi {
  width: 120px;
  height: 80px;
}

/* 过渡动效 */
.skeleton-container {
  transition: opacity 400ms ease;
}

.skeleton-container.hidden {
  opacity: 0;
  pointer-events: none;
}

.real-content {
  opacity: 0;
  transition: opacity 600ms ease 200ms; /* delay 200ms 避免闪烁 */
}

.real-content.visible {
  opacity: 1;
}
```

**粒子占位生成**：JS 在初始化时生成 30–40 个随机位置的 `.skeleton-dot`，模拟粒子星云轮廓。

### 12.2 加载失败降级方案

| 场景 | 降级 UI | 技术实现 |
|------|---------|----------|
| **JSON 加载失败（网络错误）** | 保留页面骨架结构，视口中央显示错误卡片：⚠️ 图标 + 「数据加载失败」 + 「请检查网络连接后重试」 + 「重新加载」按钮 | `fetch` 的 `catch` 分支渲染错误卡片组件 |
| **JSON Schema 不匹配** | 同上错误卡片，副文案改为「数据格式异常，请联系维护者」，附 `error code: SCHEMA_MISMATCH` 等宽字体标签 | `DataService.init()` 中 schema 校验失败时触发 |
| **部分 detail shard 加载失败** | 弹窗内降级：显示「该电影详情暂不可用」，保留基础信息（片名、评分、年份来自主 JSON），隐藏详细字段 | `detailShard()` 的 `catch` 分支 |
| **超时** | 错误卡片 + 「当前网络较慢，建议刷新重试」 | `AbortController` 设置 15 秒超时 |

**错误卡片样式**

```css
.error-card {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  max-width: 420px;
  padding: var(--space-xl);
  background: var(--color-surface-2);
  backdrop-filter: blur(16px) saturate(1.4);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-dialog);
  text-align: center;
  z-index: var(--z-toast);
}

.error-icon {
  font-size: 3rem;
  margin-bottom: var(--space-md);
}

.error-title {
  font-family: var(--font-serif);
  font-size: 1.4rem;
  color: var(--color-text-high);
  margin-bottom: var(--space-sm);
}

.error-subtitle {
  font-family: var(--font-sans);
  font-size: 0.9rem;
  color: var(--color-text-low);
  margin-bottom: var(--space-lg);
}

.error-retry-btn {
  display: inline-block;
  padding: 12px 32px;
  border: 1px solid var(--color-amber);
  border-radius: var(--radius-pill);
  color: var(--color-amber);
  font-family: var(--font-sans);
  cursor: pointer;
  transition: all var(--duration-normal) ease;
}

.error-retry-btn:hover {
  background: var(--color-amber);
  color: var(--color-surface-1);
}

.error-code {
  font-family: 'Courier New', monospace;
  font-size: 0.75rem;
  color: var(--color-text-low);
  background: rgba(255, 255, 255, 0.05);
  padding: 4px 8px;
  border-radius: var(--radius-sm);
  margin-top: var(--space-md);
  display: inline-block;
}
```

**超时实现**

```js
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 15000);

try {
  const response = await fetch('../data/frontend_dataset.json', {
    cache: 'no-store',
    signal: controller.signal
  });
  clearTimeout(timeoutId);
  // ... 处理响应
} catch (error) {
  clearTimeout(timeoutId);
  if (error.name === 'AbortError') {
    showErrorCard('当前网络较慢，建议刷新重试', 'TIMEOUT');
  } else {
    showErrorCard('数据加载失败，请检查网络连接后重试', 'NETWORK_ERROR');
  }
}
```

### 12.3 空状态与边界状态

| 场景 | 设计 |
|------|------|
| **探索舱筛选无结果** | 图表区显示空状态插画（简笔线条风格的空放大镜，SVG 120x120px，灰色 `#888`）+ 主文案「当前筛选条件下没有匹配的电影」（1.1rem，居中）+ 副文案「调整筛选条件试试」（0.9rem，灰色）+ 「重置筛选」按钮（琥珀色边框） |
| **随机打捞无匹配** | 按钮短暂禁用（`disabled` 2s）+ tooltip「暂无可打捞的方言片」 |
| **KPI 数字加载态** | 使用 `--` 占位（现有代码已部分实现），加载完成后数字使用 count-up 动效（从 0 滚动到目标值，800ms，`ease-out-quint`） |

**KPI count-up 动效实现**

```js
function animateCountUp(element, target, duration = 800) {
  const start = 0;
  const startTime = performance.now();
  
  function update(currentTime) {
    const elapsed = currentTime - startTime;
    const progress = Math.min(elapsed / duration, 1);
    // ease-out-quint
    const eased = 1 - Math.pow(1 - progress, 5);
    const current = Math.round(start + (target - start) * eased);
    element.textContent = current.toLocaleString('zh-CN');
    
    if (progress < 1) {
      requestAnimationFrame(update);
    }
  }
  
  requestAnimationFrame(update);
}
```

---

## 13. 移动端响应式设计规范

### 13.1 屏幕断点体系

| 断点名 | 宽度范围 | 布局策略 |
|--------|---------|----------|
| `desktop-xl` | ≥1440px | 完整体验：双栏 scene-lab + 全屏粒子 + 侧边目录 |
| `desktop` | 1024–1439px | 标准体验：单栏 + 全屏粒子 + 侧边目录折叠 |
| `tablet` | 768–1023px | 粒子保留但降密度；图表转单列；控件面板折叠为底部抽屉 |
| `mobile` | 480–767px | 粒子降至静态背景图；图表全纵向；控件内联；隐藏侧边目录改为顶部汉堡菜单 |
| `mobile-sm` | <480px | 极简模式：进一步缩减内边距；KPI 卡片改为 2×2 网格；隐藏非关键装饰元素 |

```css
/* 断点媒体查询 */
@media (max-width: 1439px) { /* desktop */ }
@media (max-width: 1023px) { /* tablet */ }
@media (max-width: 767px) { /* mobile */ }
@media (max-width: 479px) { /* mobile-sm */ }
```

### 13.2 粒子引擎性能降级策略

| 设备等级 | 判定条件 | 粒子数上限 | 动效降级 |
|----------|---------|-----------|----------|
| **高性能** | `navigator.hardwareConcurrency ≥ 8` 且 `deviceMemory ≥ 8` | 63,025（全量） | 完整漂浮 + 场景过渡动效 |
| **中性能** | `hardwareConcurrency ≥ 4` | 20,000（降采样） | 漂浮动效保留，场景过渡缩短至 300ms |
| **低性能** | `hardwareConcurrency < 4` 或 `deviceMemory < 4` | 8,000（降采样） | 仅保留颜色映射，关闭粒子大小动画 |
| **移动端** | `matchMedia('(pointer: coarse)')` 或 UA 检测 | 5,000 或静态快照 | 粒子渲染为静态 Canvas 快照（每场景切换时重绘一次），不运行 `requestAnimationFrame` 循环 |
| **省电模式** | `navigator.getBattery()` 返回 `charging: false && level < 0.2` | 3,000 | 同低性能 |

**硬性约束**：方言粒子的琥珀色点缀效果在任何降级等级下都必须保留。

**设备检测实现**

```js
function getDeviceTier() {
  const cores = navigator.hardwareConcurrency || 2;
  const memory = navigator.deviceMemory || 4;
  const isMobile = matchMedia('(pointer: coarse)').matches;
  
  if (isMobile) return 'mobile';
  if (cores >= 8 && memory >= 8) return 'high';
  if (cores >= 4) return 'medium';
  return 'low';
}

const PARTICLE_LIMITS = {
  high: 63025,
  medium: 20000,
  low: 8000,
  mobile: 5000
};
```

### 13.3 图表与控件重排规则

| 组件 | 桌面端（≥1024px） | 移动端（<768px） |
|------|-------------------|------------------|
| **scene-lab 面板** | 左侧固定 280px | 底部抽屉（上滑展开，高度 40vh），默认显示 insight 文本，控件折叠在「调整条件」按钮后 |
| **数据卡 `.scene-metrics`** | 4 列网格 | 2×2 网格 |
| **年代切换按钮** | 水平药丸排列 | 水平滚动（`overflow-x: auto`），隐藏溢出部分用渐变遮罩提示 |
| **烂片阈值滑块** | 全宽水平 | 全宽，数值标签移至滑块上方 |
| **个案对比双栏卡片** | 左右并排 | 上下堆叠，卡片间距 16px |
| **ECharts 图表** | 宽高自适应 | 强制 `height: 300px`；图例移至底部水平滚动；tooltip 改为点击触发（非悬停） |
| **侧边章节目录** | 固定左侧 48px | 隐藏，改为顶部汉堡菜单 + 全屏覆盖导航 |

**底部抽屉实现**

```css
.scene-lab-drawer {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 40vh;
  background: var(--color-surface-1);
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  transform: translateY(calc(100% - 60px)); /* 默认收起，仅露出 60px */
  transition: transform var(--duration-normal) var(--ease-out-quint);
  z-index: var(--z-nav);
}

.scene-lab-drawer.open {
  transform: translateY(0);
}

.scene-lab-drawer-handle {
  width: 40px;
  height: 4px;
  background: var(--color-text-low);
  border-radius: 2px;
  margin: var(--space-sm) auto;
  cursor: pointer;
}
```

---

## 14. 无障碍设计规范

### 14.1 色彩对比度规范

| 元素 | 前景色 | 背景色 | 对比度 | WCAG AA 达标 |
|------|--------|--------|--------|---------------|
| 正文 | `#E2E2E2` | `#050507` | 17.4:1 | ✅ 通过 |
| 弱化文字 | `#888888` | `#050507` | 5.9:1 | ✅ 通过 |
| 琥珀色 | `#d4a574` | `#050507` | 8.2:1 | ✅ 通过 |
| 灰蓝粒子 | `#8899aa` | `#050507` | 6.3:1 | ✅ 通过 |
| scale-tag `infer` 文字 | `#b8874a` | `rgba(212,165,116,0.15)` | 4.8:1 | ✅ 通过（加深后） |

**规范**：所有交互元素（按钮、标签、链接）的默认态和 hover/focus 态均需满足 WCAG 2.1 AA 级 4.5:1 对比度。

### 14.2 键盘导航支持

| 交互元素 | 键盘操作 | 实现方式 |
|---------|---------|----------|
| **粒子 Canvas** | Tab 聚焦后，方向键在粒子间移动焦点（按空间邻近关系），Enter 打开详情弹窗 | Canvas 添加 `tabindex="0"` + `role="application"`（已有）；JS 监听键盘事件维护焦点粒子索引 |
| **年代/口径按钮** | Tab/Enter/Space | 原生 `<button>` 元素；添加 `aria-pressed` 状态（部分已有） |
| **滑块** | 方向键 | 原生 `<input type="range">`（已有） |
| **标签页（探索舱）** | 左右方向键切换标签，Tab 键进入面板内容 | WAI-ARIA Tabs 模式：`role="tablist"` + `role="tab"` + `aria-selected` |
| **详情弹窗** | Escape 关闭 | `<dialog>` 元素（已有），焦点锁定在弹窗内（native dialog 自带） |
| **章节目录** | Tab 聚焦，Enter 跳转 | 每个锚点链接可 Tab 聚焦；当前幕的目录项添加 `aria-current="true"` |

### 14.3 屏幕阅读器替代文本

| 图表类型 | 替代文本策略 |
|---------|---------------|
| **粒子 Canvas** | 在 Canvas 旁放置 `.sr-only` 区域，包含当前场景的结构化描述。例如："当前展示 63,025 部电影的评分分布。中国电影中方言电影 3,045 部，均分 6.62，以琥珀色显示；普通话·非方言电影 9,813 部，均分 6.11，以灰蓝色显示。方言烂片率 6.4%，普通话 24.4%。" 每次场景切换时由 JS 动态更新 |
| **ECharts 图表** | 使用 ECharts 的 `aria` 组件（`option.aria = { enabled: true, showHeader: true }`），自动生成图表的 ARIA 标签。同时为每个图表的容器添加 `role="img"` + `aria-label` 描述图表核心发现 |
| **数据卡片** | 使用 `aria-live="polite"`（部分已有），确保筛选器变更时屏幕阅读器能播报新数据 |
| **scale-tag** | 每个 scale-tag 添加 `aria-label` 说明证据等级，如 `aria-label="证据等级：数据直接证明"` |

**`.sr-only` 样式**

```css
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

**动态更新实现**

```js
// 场景切换时更新 Canvas 替代文本
function updateCanvasDescription(sceneData) {
  const description = document.getElementById('canvas-description');
  description.textContent = `当前展示 ${sceneData.total} 部电影的评分分布。` +
    `方言电影 ${sceneData.dialect.count} 部，均分 ${sceneData.dialect.mean}，以琥珀色显示；` +
    `普通话电影 ${sceneData.mandarin.count} 部，均分 ${sceneData.mandarin.mean}，以灰蓝色显示。`;
}
```

### 14.4 其他无障碍要求

| 维度 | 规范 |
|------|------|
| **焦点样式** | 所有可交互元素的 `:focus-visible` 样式：`outline: 2px solid #d4a574; outline-offset: 2px`，不使用 `outline: none` |
| **跳过导航** | 页面顶部添加 "跳过导航，直达内容" 的隐藏链接（Tab 首次聚焦时显示） |
| **图片替代** | 所有装饰性图片添加 `alt=""`；信息性图片添加描述性 `alt` |
| **语言声明** | `<html lang="zh-CN">`（已有） |
| **动效开关** | 尊重 `prefers-reduced-motion: reduce`（已有基础实现），需扩展至覆盖粒子收束、场景过渡等新增动效 |

**跳过导航样式**

```css
.skip-link {
  position: absolute;
  top: -40px;
  left: 0;
  background: var(--color-amber);
  color: var(--color-surface-1);
  padding: 8px 16px;
  z-index: var(--z-toast);
  font-family: var(--font-sans);
  text-decoration: none;
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  transition: top var(--duration-fast) ease;
}

.skip-link:focus {
  top: 0;
}
```

**`prefers-reduced-motion` 扩展**

```css
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  
  /* 粒子收束动效禁用 */
  .finale-convergence {
    display: none;
  }
}
```

---

## 15. 分享与传播设计

### 15.1 分享卡片生成

**触发时机**：读者滚动至终章收尾金句后，底部浮现分享区域。

**卡片规格**

| 参数 | 值 |
|------|-----|
| 尺寸 | 1200×630px（OG Image 标准） |
| 背景 | 深色渐变 `#0d0d0d → #1a1a1e` |
| 左侧装饰 | 粒子星云（琥珀色点阵，30–40 个圆点随机分布） |
| 右侧文案区 | 主标题「方言电影为什么"赢"了？」+ 核心数据「方言烂片率 6.4% vs 普通话 24.4%」+ 收尾金句「好故事的土壤，我们从不缺」+ 二维码/URL |
| 字体 | 思源宋体（标题）+ 思源黑体（正文） |
| 生成方式 | Canvas 动态绘制，支持下载为 PNG |

**Canvas 绘制实现**

```js
async function generateShareCard() {
  const canvas = document.createElement('canvas');
  canvas.width = 1200;
  canvas.height = 630;
  const ctx = canvas.getContext('2d');
  
  // 背景渐变
  const gradient = ctx.createLinearGradient(0, 0, 1200, 630);
  gradient.addColorStop(0, '#0d0d0d');
  gradient.addColorStop(1, '#1a1a1e');
  ctx.fillStyle = gradient;
  ctx.fillRect(0, 0, 1200, 630);
  
  // 左侧粒子装饰
  for (let i = 0; i < 40; i++) {
    ctx.beginPath();
    ctx.arc(
      Math.random() * 400,
      Math.random() * 630,
      Math.random() * 3 + 1,
      0, Math.PI * 2
    );
    ctx.fillStyle = `rgba(212, 165, 116, ${Math.random() * 0.5 + 0.2})`;
    ctx.fill();
  }
  
  // 右侧文案（省略具体绘制代码）
  // ... 标题、数据、金句、二维码
  
  return canvas.toDataURL('image/png');
}
```

### 15.2 社交分享 UI

**分享按钮组**

| 按钮 | 图标 | 行为 |
|------|------|------|
| 微信 | 微信 logo | 生成二维码弹窗（使用 `qrcode.js`） |
| 微博 | 微博 logo | 跳转 `http://service.weibo.com/share/share.php?url=...&title=...` |
| 复制链接 | 链接图标 | 复制当前 URL 到剪贴板，显示「已复制」tooltip |
| 下载卡片 | 下载图标 | 下载生成的分享卡片 PNG |

**按钮样式**

```css
.share-buttons {
  display: flex;
  gap: var(--space-md);
  justify-content: center;
  margin-top: var(--space-lg);
}

.share-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--color-surface-2);
  border: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all var(--duration-normal) var(--ease-out-quint);
}

.share-btn:hover {
  background: var(--color-amber);
  border-color: var(--color-amber);
}

.share-btn svg {
  width: 20px;
  height: 20px;
  fill: var(--color-text-mid);
  transition: fill var(--duration-normal) ease;
}

.share-btn:hover svg {
  fill: var(--color-surface-1);
}
```

**"分享提示"触发**

在终章金句下方 80px 处，一行小字淡入：

```css
.share-prompt {
  text-align: center;
  font-family: var(--font-sans);
  font-size: 0.9rem;
  color: var(--color-text-low);
  margin-top: 80px;
  opacity: 0;
  transition: opacity var(--duration-slow) var(--ease-out-quint);
}

.share-prompt.visible {
  opacity: 1;
}
```

文案：「觉得这个发现有意思？分享给朋友看看」

**OG Meta 标签配置**

```html
<meta property="og:title" content="方言电影为什么"赢"了？">
<meta property="og:description" content="63,025 部电影数据揭示：方言烂片率 6.4% vs 普通话 24.4%。不是方言赢了，是好故事赢了同质化。">
<meta property="og:image" content="/share-card.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta name="twitter:card" content="summary_large_image">
```

**预生成分享文案**

| 类型 | 文案 |
|------|------|
| 数据冲击型 | 「63,025 部电影告诉我：方言片烂片率不到普通话的三成」 |
| 悬念型 | 「方言电影为什么'赢'了？答案不是方言」 |
| 情感型 | 「好故事的土壤，我们从不缺」 |

---

## 16. 组件设计规范

### 16.1 详情弹窗

**尺寸与样式**

```css
.movie-detail-dialog {
  max-width: 520px;
  max-height: 80vh;
  overflow-y: auto;
  background: rgba(13, 13, 13, 0.92);
  backdrop-filter: blur(20px) saturate(1.4);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 16px;
  box-shadow: var(--shadow-dialog);
  padding: var(--space-xl);
  /* 入场动效 */
  opacity: 0;
  transform: scale(0.92);
  transition: opacity 300ms ease-out,
              transform 300ms ease-out;
}

.movie-detail-dialog[open] {
  opacity: 1;
  transform: scale(1);
}

/* 背景遮罩 */
.movie-detail-dialog::backdrop {
  background: rgba(0, 0, 0, 0.6);
  opacity: 0;
  transition: opacity 200ms ease;
}

.movie-detail-dialog[open]::backdrop {
  opacity: 1;
}
```

**内容布局**

```
┌─────────────────────────────────────┐
│  片名（思源宋体 1.4rem）      × │  ← 关闭按钮
├─────────────────────────────────────┤
│                                     │
│  评分（大字号琥珀色 2.4rem）        │
│  年份 · 语言标签 · 类型标签        │  ← 标签使用 pill 样式
│                                     │
├─────────────────────────────────────┤
│  评价人数 · 地区 · 简短描述        │
└─────────────────────────────────────┘
```

**标签 pill 样式**

```css
.movie-tag {
  display: inline-block;
  padding: 4px 12px;
  border-radius: var(--radius-pill);
  background: rgba(255, 255, 255, 0.08);
  font-family: var(--font-sans);
  font-size: 0.75rem;
  color: var(--color-text-mid);
  margin-right: var(--space-xs);
}

.movie-tag.dialect {
  background: var(--color-amber-dim);
  color: var(--color-amber);
}
```

**弹窗内加载态**

使用 `aria-busy="true"`（已有实现），显示 3 行骨架条：

```css
.detail-skeleton {
  height: 16px;
  background: linear-gradient(90deg, #1a1a1e 25%, #252528 50%, #1a1a1e 75%);
  background-size: 200% 100%;
  animation: shimmer 1.8s infinite;
  border-radius: var(--radius-sm);
  margin-bottom: var(--space-sm);
}

.detail-skeleton:nth-child(1) { width: 70%; }
.detail-skeleton:nth-child(2) { width: 50%; }
.detail-skeleton:nth-child(3) { width: 60%; }
```

### 16.2 字体加载策略

**加载方式**

```css
@font-face {
  font-family: 'Noto Serif SC';
  font-style: normal;
  font-weight: 400;
  font-display: swap; /* 避免 FOIT */
  src: local('Songti SC'),
       local('Noto Serif SC'),
       url('/fonts/noto-serif-sc-subset.woff2') format('woff2');
}

@font-face {
  font-family: 'Noto Sans SC';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: local('PingFang SC'),
       local('Noto Sans SC'),
       url('/fonts/noto-sans-sc-subset.woff2') format('woff2');
}
```

**子集化方案**

中文 Web Font 体积大（通常 2–8MB），需使用 `fontmin` 或 `cn-font-split` 对 Noto Serif SC 做子集化，仅保留文案中用到的字符（约 500–800 字），将体积降至 50–100KB。

```bash
# 使用 fontmin 子集化
npx fontmin --subset "方言电影为什么赢了当普通话工业化量产稀释了品质选择方言的创作者以更自觉的差异化守住了华语电影的下限..." fonts/noto-serif-sc.ttf fonts/noto-serif-sc-subset.ttf

# 或使用 cn-fontsplit
npx cn-fontsplit -i fonts/noto-serif-sc.ttf -o fonts/noto-serif-sc-subset.ttf
```

**预加载**

```html
<link rel="preload" as="font" type="font-woff2" href="/fonts/noto-serif-sc-subset.woff2" crossorigin>
<link rel="preload" as="font" type="font-woff2" href="/fonts/noto-sans-sc-subset.woff2" crossorigin>
```

**降级字体栈**

```css
--font-serif: 'Noto Serif SC', 'Songti SC', 'STSong', 'SimSun', serif;
--font-sans: 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', 'Hiragino Sans GB', sans-serif;
```

---

> 本文档为开发执行蓝图，所有数据点均来自 `derived_movies.csv` 实测核算，可直接作为前端文案与可视化规格的输入。
