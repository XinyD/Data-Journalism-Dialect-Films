# 方言电影定义 — 规则口径 v4.1 · 数据基线 v4.6

> **文件状态**：判定规则于 2026-08-15 定稿（**规则口径 v4.1**）；数据基线于 2026-08-30 更新为 **v4.6（豆瓣语言回填）**。v4.1 之后的版本号只描述数据快照（空语言补全、delivery 合并、戏曲审计、阿嬷补收、Region 口径、语言回填），**不改判定规则**。
> **定位**：本文件是"中国方言电影数据故事"项目**数据清洗、报告生成与前端叙事的统一定义依据**。它固化了 v2.1 严格中国语言标准、v3 文献综述的操作口径、2026-08-15 数据清洗阶段的全部已确认规则（方案 A 第一语言检查、审计修正、F1/F6 标签治理），以及 **v4.1 新增的 Tier 2b 证据审查规则（§5.5）**。
> **与代码的关系**：本文件是规则的**文档化定稿**；可执行实现以 `scripts/dialect_defs.py` 为唯一代码事实来源（SSOT）。**现行计数与指纹以 `scripts/freeze_constants.py` 为单一常量源**，可执行验收为 `python scripts/verify_freeze_readiness.py`（勿以本节手填数字代替脚本）。Tier 2b 证据审查的执行链路为 `scripts/score_tier2b.py` + `scripts/llm_judge_tier2b.py` + `scripts/apply_tier2b_reclassify_20260815.py`。任何口径调整的唯一合法路径是修订对应代码并同步本文件与基线断言，禁止"文档口径/代码口径"双轨并存。
> **与 v4.0 的关系**：v4.0 的全部判定规则（白名单、方案 A、审计名单、Tier 决策树）原样继承；v4.1 仅对 **Tier 2b（普通话排首位 + 含方言标签）** 增加证据审查环节，其余层级判定不变。v4.0 文档保留为历史文献。v4.5 只改 Region（空格分隔制片国取第一出品国）。v4.6 按豆瓣「语言」回填 2020–2026 缺列，不改判定规则。

---

## 1. 定义摘要（可直接引用）

> **在本项目中，"方言电影"指豆瓣"语言"字段中显式标注了至少一个中国境内语言变体标签（汉语方言或中国少数民族语言），且归一化后首个语言标签不是外语的影片。**
>
> 其中，汉语方言覆盖十大方言区（粤、闽、吴、客、湘、赣、晋、徽、平话、官话地域变体）与台语（闽南语台湾变体）；中国少数民族语言指在中国境内有族裔社区使用的语言（藏语、维吾尔语、蒙古语、哈萨克语、苗语、彝语、朝鲜族朝鲜语等）。外语、手语、戏曲声腔、地方口音的普通话均不属于本项目的方言研究对象。按语言标签的有无与排序，方言电影进一步分为 **Tier 1（纯方言片）、Tier 2a（方言排首位的混合片）、Tier 2b（普通话排首位的混合片，须经 v4.1 证据审查补回）** 三个层级，其余影片为**非方言**对照组。
>
> **v4.1 关键修订**：Tier 2b 是"语言字段信号最弱"的一层（普通话排首、方言仅为次要标签），其中混有大量假阳性（如邵氏国语配音港片仅因带"粤语"标签被误纳）。自 v4.1 起，Tier 2b **默认不计入方言片**，仅通过"证据漏斗 + 逐部补判 + 人工复核"三重审查进入白名单的影片才保留（见 §5.5）。

---

## 2. 学术依据

本定义继承学术共识的**三层框架**（量、质、地域特征），并针对豆瓣元数据的可操作性做了代理化落地。完整文献综述与逐条引文见《方言电影定义_文献综述与操作标准_v3.html》。

| 学术要素 | 主要文献依据 | 本项目落地方式 |
| --- | --- | --- |
| 对白本位：方言须为人物对白，与取景地、导演籍贯、故事背景地无关 | 李恒 2009；麦剑芬 2016 | 仅依据豆瓣"语言"字段判定，不使用制片地/剧情简介 |
| 量门槛："一定数量"的对白，非点缀性出现 | 麦剑芬 2016 | Tier 分层作代理指标（标签存在性 + 排序）；第一语言检查排除外语为主影片；**Tier 2b 证据审查**进一步剔除标签点缀型假阳性 |
| 质的维度：方言承担叙事功能，非装饰 | 杨哲芬 2017；戴岭、路宝君 2007；Hodson 2014 | 元数据不可测 → 以 Tier 1（纯方言）为最强信号；Tier 2b 借助简介/Gemini评价文本证据与逐部补判逼近"质"的判定 |
| 地域特征：方言与地域文化、人群、自然三位一体（核心界定） | 陈颖 2012；金丹元、徐文明 2008；Lu & Yeh 2005 | 白名单限定中国境内语言；标签→方言大区映射（`DIALECT_GROUPS` 17 组） |
| 狭义口径：排除地方戏曲片 | 李恒 2009 广义/狭义之分 | 排除戏曲声腔标签（京剧电影/黄梅戏/曲剧等） |
| 少数民族语言归属：功能上与方言同构 | v2.1 确立（2026-08-10） | 中国境内少数民族语言视同方言研究对象 |
| 区别于 accented cinema（口音/流散电影） | Naficy 2001；Lu & Yeh 2005 | 严格限定中国语言内部变体，外语一律排除 |
| 标准语对照系 | 语言学共识（Trudgill 1974 等） | "汉语普通话/国语"归一化为"普通话"作对照组；"台湾国语"独立保留 |
| 影视使用方言的合法性背景 | 《国家通用语言文字法》（2000）第十六条第（三）项 | 佐证"以普通话为基本用语、方言为艺术例外"的结构 |

**排除案例（学术反例）**：《硬汉》中说河南话的外地人仅为配角、用于衬托主角——属"量的维度"不达标的点缀性使用，不构成方言电影（麦剑芬 2016"一定数量"门槛）。

---

## 3. 纳入标准

一部影片判为方言电影（`Is_Dialect = 1`），须**同时**满足：

1. **制片地区属中国口径**：`Region = China`（本项目统计范围限中国制片电影；v4.5 第一制片国口径下全量 63,025 行中 China 共 **12,791** 部）。
2. **显式方言标签**：豆瓣"语言"字段按分隔符（`/`、`|`、`;`、`；`、`,`）切分并归一化后，至少 1 个标签命中方言白名单 `DIALECT_MARKERS_STRICT`（**224 个标签、17 个大区分组**，见 §5.3）。
3. **通过第一语言检查（方案 A）**：语言字段**首个标签**（归一化后）不命中外语标记 `FOREIGN_MARKERS`（117 个外语标记）。首标签为外语者判为外语为主影片，移出口径（`Is_Dialect = 0`）。
4. **（v4.1 新增）若落入 Tier 2b（含普通话标签且普通话排首位），须通过证据审查**：`derived_movies.csv` 的 `Dialect_Evidence` 列记录了补回证据（`E:E1/E2/…`、`BENCHMARK`、`LLM_JUDGE`）；证据为空或为 `TIER2B_EXCLUDED` / `LANG_BACKFILL_TIER2B_EXCLUDED` 者一律 `Is_Dialect = 0`（见 §5.5）。

**不做任何间接推断**：v1 时代的间接规则（"含中文标签 + 多语言"即判方言，即 Tier 1\*，曾误纳 784 部）已于 v2.1 删除，**不得恢复**。

---

## 4. 排除标准

出现下列任一情形，**必须**排除出方言口径：

| 编号 | 排除情形 | 判定方式 | 来源依据 |
| --- | --- | --- | --- |
| E1 | **外语**：英语、日语、韩语（한국어）、法语、德语、意大利语、西班牙语、俄语、泰语、越南语等 | 外语标签不触发方言判定；白名单 `FOREIGN_MARKERS`（117 个标记） | v2.1 |
| E2 | **外语为主（第一语言检查，方案 A）**：命中方言标签但语言字段首位标签为外语 | `first_tag_is_foreign()` 命中 → `Is_Dialect = 0`（实测移除 54 部：Tier 1 −21 / Tier 2b −33） | 《方言电影数据清理与修改报告》修改建议二；方案 A 落地（2026-08-15） |
| E3 | **手语**：手语 / sign language / 台湾手语 | 视觉-手势语言模态，非口头方言，单列研究 | v2.1 |
| E4 | **戏曲声腔**：京剧电影、黄梅戏、粤剧、越剧、豫剧、曲剧、昆曲等一切剧种的戏曲片 | 表演艺术程式（韵白/板式/曲牌），非日常对白语言（狭义口径）；2026-08-18 审计已将 10 部因方言标签误纳的戏曲片置 `Is_Dialect = 0`（`Dialect_Evidence = AUDIT_EXCLUDED_OPERA_CONCERT`，名单 `OPERA_CONCERT_EXCLUDE_MOVIE_IDS`）；实证论证见 §4.1 | 李恒 2009；v2.1；2026-08-18 审计（用户审核确认） |
| E5 | **国别歧义假阳性**：①境外"XX 方言"兜底标签误命中（南部瑞典方言、印度方言、剛果方言、坦纳岛西南部方言，共 4 部）；②"朝鲜语"歧义（17 部朝鲜半岛影片被误判为中国朝鲜族语言片） | 人工审计排除名单 `DIALECT_AUDIT_EXCLUDE_MOVIE_IDS`（22 个 movie_id，已固化于 `dialect_defs.py`；从源数据重建时必须应用） | 2026-08-15 阶段一审计（用户审核确认） |
| E6 | **地方口音的普通话**（港普、带川音的普通话等） | 词汇语法仍是普通话，仅发音偏移 → 不算方言；标签层面无信号，人工核验时以"关掉字幕是否追得上"口诀区分 | 轻量 Codebook §1.3 |
| E7 | **无豆瓣评分的影片** | 评分统计口径要求分母无缺失（无评分片不得进入均分/烂片率分母） | 审计决定（《The Court-Martial of Jackie Robinson》已删） |
| E8 | **（v4.1 新增）Tier 2b 未通过证据审查**：普通话排首 + 方言标签，但无补回证据 | 证据漏斗三分类 `exclude`（215 部）或灰区补判 `no`（109 部）/`uncertain`（29 部），另含用户人工复核移出 1 部（《芒种》，2026-08-15）→ `Is_Dialect = 0`、`Dialect_Evidence = TIER2B_EXCLUDED`（合计 354 部） | v4.1 Tier 2b 证据审查（2026-08-15，用户决策：港片群默认排除-保守） |
| E9 | **（2026-08-18 新增）戏曲片与演唱会/音乐纪录片/颁奖典礼**：①戏曲片 10 部（含粤剧《蝶影红梨记》，E4 的执行落地）；②演唱会/音乐会纪录片 21 部；③音乐纪录片/音乐盛典 9 部；④金像奖颁奖典礼真人秀 9 部 | 非叙事影片，方言（多为粤语歌曲/舞台声腔）不承担叙事功能，不满足量/质/地域三层定义；且演唱会片评分系统性偏高（均值 9.5+），会抬高方言组均分。人工审计排除名单 `OPERA_CONCERT_EXCLUDE_MOVIE_IDS`（49 个 movie_id，固化于 `dialect_defs.py`）→ `Is_Dialect = 0`，影片保留在数据集（普通话组） | 2026-08-18 戏曲/演唱会审计（用户审核确认）；先例：2026-08-16 语言补全时张学友演唱会已按非方言处理；同时修复 OPERA_MARKERS 定义后从未在判定逻辑中使用的执行缺口 |

**高危歧义对**（判定前先确认归属）：

- **朝鲜语 ≠ 韩语**：延边等中国朝鲜族社区使用的"朝鲜语/朝鮮語" → **纳入**（少数民族组）；朝鲜半岛制片、标签为"韩语/韓語/한국어" → **排除**（外语组）。
- **蒙古语/哈萨克语**：中国少数民族语言 → 纳入；蒙古国、哈萨克斯坦制片 → 依制片地区属外语片，排除。
- **台语 ≠ 台湾国语**：台语（闽南语台湾变体）→ 纳入方言；台湾国语是普通话的地域变体 → **不纳入**（归对照组，独立标签保留）。
- **（v4.1）港片"汉语普通话/粤语"双标签**：217 部双标签港片含大量邵氏国语配音片（对白实为普通话配音，"粤语"标签仅表示发行版本）→ 默认走 E8 排除通道，真实粤语原声片须经证据审查补回。

### 4.1 E4 补充论证：为什么"原声戏曲片"也不计入（2026-08-18）

曾有备选口径建议：戏曲片应区分剧种——京剧（程式化韵白）排除，粤剧/越剧/川剧/黄梅戏等以地方原生方言演唱的原声戏曲片计入方言电影。本项目经论证**不采纳**该口径，维持 E4 全类排除，理由如下：

1. **学理：排除理由对全部声腔同样成立（定义自洽性）**。备选口径排除京剧的理由是"韵白是舞台加工过的戏白，不是现实生活口语方言"——该理由可逐字推广到所有剧种：越剧声腔 = 嵊州方言底 + 中州韵韵脚体系，豫剧、粤剧、黄梅戏均有韵白/曲牌/咬字归韵等程式化规范。戏曲声腔整体属"程式化舞台腔"，其语言模态是唱念表演而非"人物用方言说话"。若以"有方言基底"为由计入，则京剧（徽汉合流、韵白体系）同样有方言基底，边界画在京剧上属任意切割。故按李恒 2009 狭义口径整体排除。
2. **实证：戏曲片高分属"经典效应"而非"方言效应"（统计污染防御）**。实测（全量 63,025 部，`_check_opera_comparison.py` 可复现）：类型含"戏曲"的 118 部影片均分 **8.17**、高分率（≥8）**69.5%**，而 China 口径方言组（n=3,337）均分仅 **6.66**、高分率 **10.3%**。备选口径想计入的 10 部戏曲片均分高达 **8.56**、9/10 部 ≥8 分（《帝女花》9.4、《蝶影红梨记》9.1、《五女拜寿》9.0、《抬花轿》9.0）。戏曲片在豆瓣的高分来自经典保留剧目的艺术地位与戏迷打分人群（幸存者偏差），若计入将把"方言片赢在质量"的核心结论污染为"经典戏曲赢在艺术地位"，因果归因不可追溯。2026-08-18 审计据此将 10 部戏曲片（连同 39 部演唱会/音乐纪录片/颁奖典礼）移出口径。
3. **可执行性："以公映原版音轨为准"在元数据方法下不可操作**。本项目判定仅依据豆瓣"语言"字段 + 人工审计名单（可复现）。实测 118 部戏曲片的语言字段分布：标"普通话/国语"83 部、外语或空 23 部、含方言/声腔标签仅 10 部、标"京剧"2 部——若按"是否原声"筛选，实际筛出的是"恰好被豆瓣打了方言标签的戏曲片"（如越剧《碧玉簪》标"越剧"而大量同类原声片标"普通话"），同剧种影片被标签偶然性劈成两半，口径不自洽且不可复现。
4. **边界说明（两点共识）**：① 普通话译配版戏曲片不计入——与备选口径一致；② 京剧片在本口径下同样排除（其语言字段标"京剧/普通话"，不触发白名单），两口径在此无实际分歧，真实差异仅落在上述 10 部地方戏曲片。
5. **例证核查**：备选口径的旗舰例子《白蛇传·情》（2021 粤剧电影）**不在本数据集 63,025 部中**，对该口径无实操对象；其列举的《五女拜寿》语言字段为"汉语普通话 / 吴越方言"（普通话排首的混合标签），已于 2026-08-18 审计排除。

> 一句话口径：**戏曲片需按声腔性质整体判定——一切剧种的戏曲片（含以粤、川、吴等方言为声腔基底的原声片）均属程式化表演艺术，不纳入方言电影统计；普通话译配版本同样不计。**

---

## 5. 操作化判定规则（可执行流程）

### 5.1 判定流程图（与 `dialect_defs.py` / `gen_report_strict.py` / `gen_dialect_report.py` 一致）

```
输入：豆瓣"语言"字段原文 lang（如 "粤语 / 汉语普通话"）
────────────────────────────────────────────
步骤 0  预处理
        lang_parts(lang)：normalize（去首尾空白、合并空格、casefold）后按
        / | ; ； , 切分为标签列表 parts；归一化映射 normalize_language_tags()：
        "汉语普通话/国语/中文"等 → "普通话"；"台湾国语"独立保留；
        含方言白名单子串的标签（如"汉语方言"）保留原样（方言优先，不归并）。

步骤 1  白名单命中判定 has_strict_dialect_tag(lang)
        parts 中任一标签命中 DIALECT_MARKERS_STRICT（224 标签/17 组，子串匹配）？
        ├─ 否 → Is_Dialect = 0，层级 = 非方言 → 结束
        └─ 是 → 进入步骤 2

步骤 2  第一语言检查（方案 A）first_tag_is_foreign(lang)
        首个标签（归一化后）命中 FOREIGN_MARKERS？
        ├─ 是 → Is_Dialect = 0，层级 = 非方言（外语为主）→ 结束
        └─ 否 → 进入步骤 3

步骤 3  普通话标签判定 has_mandarin_tag(lang)（方言优先完成式）
        逐 part 判定：含方言白名单子串的 part 不贡献普通话信号；
        其余 part 命中 MANDARIN_MARKERS（汉语普通话/普通话/国语/中文/台湾国语等 11 个）？
        ├─ 不含普通话标签 → Tier 1 纯方言片（强信号）
        └─ 含普通话标签 → 进入步骤 4

步骤 4  首位标签判定（Tier 2 分层）
        方言白名单标签是否出现在 parts 第 1 位（i == 0）？
        ├─ 是 → Tier 2a 方言排首位（中信号）
        └─ 否（普通话排第一）→ Tier 2b 候选 → 进入步骤 5

步骤 5  Tier 2b 证据审查（v4.1，2026-08-15）
        读取 derived_movies.csv 该行的 Dialect_Evidence 列：
        ├─ 非空且非 TIER2B_EXCLUDED（E:E1/E2/… / BENCHMARK / LLM_JUDGE）
        │       → Tier 2b 普通话排首位（弱信号，白名单保留），Is_Dialect = 1
        └─ 空 或 TIER2B_EXCLUDED → 默认排除，Is_Dialect = 0，层级 = 非方言
        （白名单生成流程见 §5.5；证据列由 apply_tier2b_reclassify_20260815.py 写回）
```

### 5.2 关键实现约束（防回归）

| 约束 | 说明 | 出处 |
| --- | --- | --- |
| 白名单唯一来源 | 任何脚本不得重复定义方言白名单，一律 `from dialect_defs import ...`；修改方言定义只改 `dialect_defs.py` 一处 | T1 白名单去重（2026-08-15） |
| 第一语言检查三处统一 | `first_tag_is_foreign()` 下沉 `dialect_defs.py`，`classify_strict` / `classify_v21` / `data_processor.language_code` 三处统一接入，防口径脱节 | 方案 A SSOT 化 |
| 方言优先规则（F1） | 含方言白名单子串的标签（如"汉语方言"含"汉语"）不归并为普通话、不贡献普通话信号——否则仅标通用方言标签的影片会被误判含普通话而降层 | F1 修复 + F1 收尾（2026-08-15） |
| 交叉校验断言 | 报告重算的 Is_Dialect 必须与 `derived_movies.csv` 的 Is_Dialect 列逐行一致，不一致即报错（防 SSOT 与主表脱节） | F3（2026-08-15） |
| 烂片率分母防御（F2） | 无评分影片（评分 ≤ 0）进入分组即断言失败，防止烂片率口径被污染 | F2（2026-08-15） |
| 审计排除名单 | 重建数据时必须应用 `DIALECT_AUDIT_EXCLUDE_MOVIE_IDS`（22 个 movie_id），防止 E5 假阳性重新混入 | 阶段二审计修正 |
| 短标记子串匹配风险 | "dai"（傣语）、"白话"等短标记使用子串匹配，当前规范标签数据下风险低；若数据源变化需升级为分词精确匹配（已记录，暂不改逻辑） | T8 / F5 风险评估 |
| **Tier 2b 证据列感知** | `classify_strict` / `classify_v21` 的 Tier 2b 分支必须读取 `Dialect_Evidence` 列，空值或 `TIER2B_EXCLUDED` → 默认排除；两判定函数行为一致并有测试锁定 | v4.1（2026-08-15） |
| **重建守卫** | `data_processor.py` 主流程检测到输出已含 `Dialect_Evidence` 列即拒绝重跑（`--overwrite-tier2b` 显式覆盖后必须重跑 `apply_tier2b_reclassify_20260815.py` 补回），防止 `language_code()` 重算撤销 Tier 2b 重分类 | v4.1（2026-08-15） |
| **重分类前备份** | Tier 2b 落地前主表已备份为 `data/derived_movies_v21_tier2b_backup_20260815.csv`（幂等，不覆盖） | v4.1（2026-08-15） |

### 5.3 白名单结构（DIALECT_MARKERS_STRICT，224 标签 / 17 组）

| 大区分组 | 代表性标签（节选） |
| --- | --- |
| 粤语（广府片/白话） | 粤语、cantonese、广东话、广州话、广西白话、白话 |
| 闽南语（闽台片） | 闽南语、hokkien、福建话、潮州话、潮汕话、汕尾话 |
| 吴语（江浙片） | 上海话、沪语、吴语、苏州话、杭州话、温州话、常州话 |
| 西南官话（川渝滇黔片） | 四川话、重庆话、武汉话、贵州话、云南话、**昆明话、独山话**（审计补收） |
| 客家话 | 客家话、hakka、客家语、闽西客家方言 |
| 湘语 | 湘语、湖南话、长沙话、湘潭话、湘西方言 |
| 赣语 | 赣语、江西话、鄱阳方言 |
| 晋语 | 晋语、山西方言、太原话、大同方言、**井陉话**（审计补收，属晋语大包片） |
| 徽语 | 徽语、徽州话、徽州方言 |
| 平话 | 平话、桂柳话 |
| 东北/冀鲁/胶辽官话 | 东北话、大连话、青岛话、山东话、唐山话 |
| 中原/兰银官话 | 河南话、陕西话、陕北方言、西安话、甘肃方言、新疆方言 |
| 江淮官话 | 南京话、徐州方言、蚌埠话、安徽方言 |
| 北京官话 | 北京话、天津话、天津方言 |
| 通用汉语方言标签 | 方言、汉语方言、中文方言、地方方言、广西方言（F6 治理：复合脏标签已拆分去重） |
| 台语（闽南语台湾变体） | 台语、臺語、taiwanese |
| 中国少数民族语言 | 藏语、维吾尔语、蒙古语、哈萨克语、苗语、彝语、壮语、傣语、侗语、瑶语、白语、哈尼语、傈僳语、佤语、拉祜语、纳西语、锡伯语、满语、朝鲜语（中国朝鲜族）等 81 个标记 |

> 完整逐标签清单见 `scripts/dialect_defs.py` 的 `DIALECT_GROUPS` 与《方言电影定义_学术标准与操作判定.html》第四节；新增标签归入对应大区即可扩展，修订须同步本文件。

### 5.4 普通话归一化规则

| 原始标签 | 归一化结果 | 理由 |
| --- | --- | --- |
| 汉语普通话 / 普通话 / mandarin | 普通话 | 现代标准汉语，对照组标准语 |
| 国语 / 國語 / 汉语 / 中文 | 普通话 | 同一语言变体的历史/地区称谓（"国语"补收于阶段二审计，修复 23 部 Language_Category 误归） |
| 台湾国语 / 臺灣國語 | 台湾国语（独立保留） | 普通话在台湾的地域变体，属官话系统，非闽南方言 |
| 含方言白名单子串的标签（如"汉语方言""中文方言"） | **保留原样，不归并** | 方言优先规则（F1），防止方言信号被剥离 |

### 5.5 Tier 2b 证据审查规则（v4.1 新增章节）

**问题背景**：Tier 2b（普通话排首 + 含方言标签）共 702 部，是信号最弱的一层。实测发现其中 217 部为"汉语普通话/粤语"双标签港片（含邵氏国语配音片——对白实为普通话配音，"粤语"仅表示发行版本），另有大量合拍片/动画配音版本仅因点缀性方言标签被误纳；但同时也包含《疯狂的石头》《心花路放》等公认方言片。**处理决策（用户确认，2026-08-15）：该群默认排除（保守），经证据审查补回者入白名单。**

#### 5.5.1 证据漏斗（`scripts/score_tier2b.py`）

对 702 部逐部打分，三分类输出 `auto_recover / gray_zone / exclude`：

| 证据 | 内容 | 权重 |
| --- | --- | --- |
| E1（强正向） | 方言标签 ≥ 2 个 | +3 |
| E2（中正向） | 单方言标签，但紧随普通话居语言字段第 2 位 | +1 |
| E3（中正向） | 剧情简介命中方言关键词（简介/评价方言关键词表） | +2 |
| E4（中正向） | Gemini评价提及方言 | +2 |
| N1（强负向） | 制片含香港 + 仅"普通话/粤语"双标签（邵氏/老港片假阳性画像） | −3 |
| N2（弱负向） | 方言标签位置 ≥ 4（排在多个外语/标准语之后，点缀性） | −1 |

**判定阈值**：score ≥ 3 → `auto_recover`（62 部）；score ≤ −2 → `exclude`（215 部）；其余 → `gray_zone`（425 部）。**标杆影片强制落入 gray_zone**（无论得分，必须经补判与人工复核通道）。逐部得分、命中证据与判定公示于 `data/tier2b_evidence.csv`。

> 注：计划阶段曾评估"取景地/评论字段"作为信号源，实测数据集中不存在这两类字段，故不作为信号源；E3/E4 使用简介与 Gemini评价作辅助文本证据。

#### 5.5.2 灰区逐部补判（`scripts/llm_judge_tier2b.py`）

- 有 `GEMINI_API_KEY` 时逐部调用 LLM（输入：片名/年份/导演/语言字段/简介/Gemini评价；输出：yes/no/uncertain + 置信度 + 理由）；无 API 时导出 `data/tier2b_gray_zone_review.csv` 供离线批量判定后回填——两种路径脚本均支持；本轮采用离线路径逐部判定 425 部。
- **判定原则**：大陆/台片明确方言主体对白 → yes；真实粤语原声港片（《无间道》《猛龙过江》《低俗喜剧》《叶问》系列等）→ yes；邵氏国语配音片、普通话主导合拍片、动画配音版本、演唱会纪录片 → no；证据不足 → uncertain（按默认排除处理）。
- **补判结果**：yes 287 / no 109 / uncertain 29；全部 425 条结论（含理由）记入 `data/review_queue.csv` 保证可溯源。

#### 5.5.3 标杆影片与人工复核

- **标杆影片清单**（学术界/公众广泛认定的方言片与数据集求交集，内置于 `score_tier2b.py` 并随证据 CSV 公示；用片名+年份消歧）：
  疯狂的石头 (2006)、疯狂的赛车 (2009)、西藏往事 (2011)、秘密基地 (2020)、亲爱的 (2014)、心花路放 (2014)、让子弹飞 (2010)、三峡好人 (2006)、站台 (2000)、任逍遥 (2002)、世界 (2004)、天注定 (2013)、山河故人 (2015)、江湖儿女 (2018)、路边野餐 (2015)、南方车站的聚会 (2019)、追凶者也 (2016)、一个勺子 (2014)、鸡犬不宁 (2006)、光荣的愤怒 (2006)、姨妈的后现代生活 (2007)。
  （实测命中 11 部；《爱情神话》等 2021 年后影片不在数据快照内，本轮无法补收，仍属既有待办。）
- **强制人工复核**：标杆影片 + LLM 置信度 < 0.7 的案例 → `human_review_required = 1`（本轮共 11 部），使用既有 `codebook_review.csv` 记录卡；其余按既有抽样机制（154 部，seed=42）抽检。

#### 5.5.4 白名单与落地

- 文件 `data/tier2b_recovered.csv`（`data/archive/analysis/` 另有哈希不同的 v4.1 快照）：**286 行 LLM 补判 yes 子集**，不是计人口径白名单。计人口径 Tier 2b = 346（见 `freeze_constants.TIER2B_MANDARIN_FIRST`）：auto_recover + LLM yes −《芒种》等人核移出 − 后续戏曲审计从补回池拆出的部次。列含 movie_id、证据类型（`E:E1/E2/…` / `BENCHMARK` / `LLM_JUDGE`）、置信度、人工复核标记、理由。
- 落地脚本 `scripts/apply_tier2b_reclassify_20260815.py`（一次性，幂等备份）：白名单保留 `Is_Dialect = 1` 并写入 `Dialect_Evidence` 列；其余 353 部 `Is_Dialect → 0`、`Language_Code 3 → 2`、`Dialect_Evidence = TIER2B_EXCLUDED`；断言不变量（code2 == Chinese&Is_Dialect=0、code3 == Is_Dialect=1）与标杆片在白名单内；重算样本指纹并同步 `sample_manifest.json`（`tier2b_20260815` 溯源块）与 README。
- **结果**：China 方言片 3,436 → 3,083（Tier 2b 净排除 353 部）→ 2026-08-18 空语言回填 +8 部方言 → 3,091 → 审计排除名单与《芒种》复核后 v4.2 终态 3,090 → 2026-08-18 戏曲/演唱会审计排除 49 部（China 46 部）→ v4.3 终态 3,044（Tier 1 2,289 / Tier 2a 409 / Tier 2b 346）→ 2026-08-19 《给阿嬷的情书》语言字段修正补收（delivery_20260817 缺语言列被默认回填普通话，对照豆瓣实页修正为潮汕话居首）→ **v4.4 终态 3,045**（Tier 1 2,289 / Tier 2a 410 / Tier 2b 346）。v4.5 第一制片国只改 Region，方言 3,045 与 Tier 分层不变。v4.6（2026-08-30）按豆瓣详情回填 2020–2026 缺语言列 → 3,066（Tier 1 2,303 / Tier 2a 417 / Tier 2b 346）；同日按豆瓣现页补收《隐入尘烟》（甘肃方言排首）→ **终态 3,067**（Tier 1 2,303 / Tier 2a 418 / Tier 2b 346）。新发现的普通话排首+方言标签默认排除（`LANG_BACKFILL_TIER2B_EXCLUDED`），不并入原 702 证据池。

---

## 6. Tier 分层说明

### 6.1 层级定义与方言占比推断口径

> ⚠️ 豆瓣"语言"字段不含对白时长占比，下表占比均为**基于标签结构的推断值（非实测）**，所有报告中一律标注"推断"。口径出处：v3 文档第五节，已固化于 `dialect_defs.py` 模块注释与 `gen_dialect_report.py` 的 `RATIO_BY_TIER`。

| 层级 | 标签结构 | 信号强度 | 方言对白占比（推断） | 推断依据 |
| --- | --- | --- | --- | --- |
| **Tier 1** 纯方言片 | 含方言/少数民族语言标签 + 不含普通话标签 | 强信号 | ≈ 100% | 李恒 2009"以方言作为全部人物对白"的狭义纯方言片 |
| **Tier 2a** 方言排首位 | 含方言 + 含普通话标签，方言排第一 | 中信号 | > 50% | 豆瓣标签首位通常对应对白主体语言（麦剑芬 2016"一定数量"且方言占优） |
| **Tier 2b** 普通话排首位（证据补回） | 含方言 + 含普通话标签，普通话排第一，**且通过 v4.1 证据审查** | 弱信号 | < 50% | 方言为次要但显著的对白成分；未通过证据审查者归入对照组（默认排除） |
| **非方言** 对照组 | 不含任何方言标签，或命中方案 A / 审计排除 / Tier 2b 默认排除 | — | 0% | 普通话主导；不排除存在未标注的少量方言对白（平台标注盲区） |

### 6.2 典型示例（取自最新数据，均为真实判定结果）

| 层级 | 示例影片 | 语言字段 | 说明 |
| --- | --- | --- | --- |
| Tier 1 | 《细路祥》（1950） | 粤语 | 唯一语言标签为方言 |
| Tier 1 | 《抓壮丁》（1963） | 四川方言 | 经典纯方言片 |
| Tier 1 | 《四个春天》（2019） | 贵州独山话 | 审计补收（白名单新增"独山话"） |
| Tier 1 | 《村戏》（2017） | 河北井陉话 | 审计补收（白名单新增"井陉话"） |
| Tier 1 | 《投奔怒海》（1982） | 粤语 / 日语 / 法语 / 越南语 | 含多个外语标签但**粤语排首**，通过第一语言检查，保留 |
| Tier 2a | 《秋菊打官司》（1992） | 陕西话 / 汉语普通话 | 学术纯方言片因豆瓣同时标注普通话被归入 Tier 2a（已知偏差，保留为方言片） |
| Tier 2a | 《小武》（1998） | 晋语 / 汉语普通话 | 同上 |
| Tier 2a | 《重庆森林》（1994） | 粤语 / 汉语普通话 / 英语 / 印地语 / 日语 | 多语混排但粤语排首，保留（方案 A 不误杀的经典片） |
| Tier 2b（补回） | 《疯狂的石头》（2006） | 汉语普通话 / 重庆话 | 标杆片，补判 BENCHMARK 补回（全程重庆话对白的公认方言片） |
| Tier 2b（补回） | 《心花路放》（2014） | 汉语普通话 / 粤语 / 云南方言 | 标杆片，补判 BENCHMARK 补回 |
| Tier 2b（补回） | 《让子弹飞》（2010） | 汉语普通话 / 四川话 / 山西话 | 证据漏斗/补判补回 |
| 非方言（方案 A） | 《卖身契》（1978） | 英语 / 粤语 | 外语排首 → 判外语为主，移出口径 |
| 非方言（Tier 2b 默认排除） | 《少林三十六房》（1978） | 汉语普通话 / 粤语 | 邵氏国语配音片，"粤语"仅为发行版本标签 → E8 排除 |
| 非方言（Tier 2b 默认排除） | 《冷血十三鹰》（1978） | 汉语普通话 / 粤语 | 同上 |

---

## 7. 边界案例处理规则

| 情形 | 稳定处理规则 | 来源 |
| --- | --- | --- |
| **外语为主 + 方言点缀（合拍片）** | 语言字段首位标签为外语 → 方案 A 移出口径（54 部）；外语标签非排首 → 保留并标注（355 部，见 `data/plan_a_foreign_annotated.csv`），不改变层级判定 | 数据清理报告 + 方案 A 决定（四方案模拟后取最保守方案，不误杀《我不是药神》《重庆森林》） |
| **多语言混排（方言+普通话+外语）** | 只要方言标签存在且通过第一语言检查，即判方言片；层级由"有无普通话标签 + 首位标签"唯一决定，不做占比实测；若落入 Tier 2b 仍须通过证据审查 | v2.1 决策树 + v4.1 §5.5 |
| **（v4.1）港片"普通话/粤语"双标签** | N1 强负向画像直接推入 exclude；真实粤语原声港片在灰区补判中逐部确认为 yes（如《无间道》《叶问》系列），配音版一律 no | 用户决策"默认排除（保守）"；灰区补判记录 |
| **（v4.1）补判 uncertain** | 按默认排除处理（计入 353 部），结论与理由仍写入 review_queue.csv，后续人工复核可翻案（翻案走白名单增补流程，禁止直接改主表） | v4.1 §5.5.2 |
| **少数民族语言 vs 外语分不清** | 以制片地区 + 说话人社区判断：中国境内族裔社区语言（延边朝鲜语、内蒙古蒙古语）→ 纳入；对应主权国家制片 → 排除；无法确认 → 进复核队列（`data/review_queue.csv`），不硬判 | 轻量 Codebook §6；审计实践 |
| **"朝鲜语"国别歧义** | "朝鲜语/朝鮮語"保留在少数民族白名单（中国朝鲜族语言）；歧义影片（Region ≠ China 或朝鲜半岛制片）按审计排除名单处理，不修改白名单本身 | 阶段一审计（17 部排除） |
| **通用"方言"兜底标签**（方言/汉语方言/中文方言/地方方言） | 保留在"通用汉语方言标签"组，命中即计方言信号；但含方言子串的通用标签**不贡献普通话信号**（方言优先），仅标通用标签的影片判 Tier 1 而非 Tier 2 | F1 收尾；F6 治理 |
| **复合脏标签**（如"汉语普通话 汉语方言""台语 taiwanese"） | 已拆分为独立语义单元并去重（F6），各单元按所属组独立命中；拆分前后计数零差异 | F6 治理（2026-08-15） |
| **境外"XX 方言"标签**（南部瑞典方言等） | 白名单不含此类标签；已误入者按审计排除名单处理（4 部）；重建数据时名单必须应用 | 阶段一审计 |
| **口音 vs 方言** | 仅发音偏移、词汇语法仍是普通话 → 口音，不算方言；词汇、语法、声调系统独立 → 方言。自动化口径无法区分（标签盲区），人工核验按 Codebook §1.3 口诀处理 | v3 元数据盲区声明；轻量 Codebook |
| **学术纯方言片被降级**（《秋菊打官司》《小武》等） | 豆瓣同时标注普通话 → 归 Tier 2a 而非 Tier 1；保留为方言片，报告中作为已知偏差声明 | v2.1 已知偏差 |
| **双版本（普通话版/方言版）** | 本项目基于豆瓣单条记录判定，不区分版本；人工核验时须在记录卡注明所看版本，不同版本分别记录不合并 | 轻量 Codebook §6 |

---

## 8. 可追溯性：规则 ↔ 来源对照表

| 规则/口径 | 来源依据 |
| --- | --- |
| 研究对象限定中国境内语言（汉语方言 + 少数民族语言） | v2.1《方言电影定义_学术标准与操作判定.html》第二节；v3 第一节学术共识 |
| 三层学术框架（量/质/地域） | v3 第三节（李恒 2009、麦剑芬 2016、杨哲芬 2017、陈颖 2012 等综合整理） |
| 白名单显式判定、无间接规则（Tier 1\* 删除） | v2.1 第三节核心变更 |
| 外语/手语/戏曲声腔排除 | v2.1 排除范围表；v3 第 4.1 节映射表 |
| 普通话/国语归一化、台湾国语独立保留 | v2.1 第五节；学术标准文档第五节 |
| Tier 1/2a/2b 决策树与信号强度 | v2.1 第三节决策树；v3 第 4.2 节 |
| 方言占比推断口径（≈100% / >50% / <50% / 0%） | v3 第五节；`dialect_defs.py` 模块注释；轻量 Codebook v2.0.1 §4 脚注 |
| 第一语言检查（方案 A） | 《方言电影数据清理与修改报告.html》修改建议二；`analyze_v21_foreign_risk.py` 四方案模拟；`scripts/history/apply_plan_a.py` 落地（规则已内置 `data_processor`）；排除清单 `data/archive/analysis/plan_a_excluded.csv`（54 部）、标注清单 `data/archive/analysis/plan_a_foreign_annotated.csv`（355 部） |
| 方言优先规则（含方言子串标签不归并/不贡献普通话信号） | F1 修复与 F1 收尾（2026-08-15）；回归测试 `tests/test_pipeline.py` |
| 复合脏标签拆分去重 | F6 治理（2026-08-15），计数零差异验证 |
| 白名单补收（昆明话/独山话/井陉话）与审计排除名单（22 个 movie_id） | 阶段一审计《待审错误清单_20260815.md》→ 用户审核 → `scripts/history/apply_audit_fix_20260815.py`；现行重建由 `apply_audit_exclude_20260818.py` 重放；溯源 `data/review_queue.csv` |
| "国语/國語"补入 CHINESE_LANGUAGE_MARKERS | 阶段二审计（修复 23 部 Language_Category 误归） |
| 冲突裁决规则（数据字段以 dialect_defs.py 为准，禁止双轨） | 轻量 Codebook v2.0.1 §8.1；Review 报告 §4.3 版本基准结论 |
| 人工核验工作流（分层抽样 154 部，seed=42） | `data/codebook_review_sample.csv`；轻量 Codebook §8 推荐工作流 |
| **Tier 2b 证据审查（默认排除 + 证据漏斗 + LLM/人工补判 + 标杆复核）** | v4.1（2026-08-15）：用户决策（港片群默认排除-保守；标杆片与低置信案例强制人工复核）；`score_tier2b.py` → `data/tier2b_evidence.csv`（702 = 62/425/215）；`llm_judge_tier2b.py --apply` → `data/tier2b_recovered.csv`（287）；425 条补判结论入 `data/review_queue.csv`；`apply_tier2b_reclassify_20260815.py` 落地（备份 `data/derived_movies_v21_tier2b_backup_20260815.csv`） |

---

## 9. 可复查性：核查清单与验收标准

### 9.1 数据一致性验收（以脚本为准）

**可执行入口（v4.6）**：`python scripts/verify_freeze_readiness.py`。现行计数与指纹只维护在 `scripts/freeze_constants.py`；数字过期时改常量，不要改这份手填清单来“验收”。下列快照仅供阅读，**不能代替脚本**。

- [ ] **三方计数一致**：`data/cleaned/derived_movies.csv`（China, Is_Dialect=1）== `data/dialect_films/report_data_strict.json` summary == `data/narrative_facts.json`。v4.6 快照：**China 方言片 3,067 / 普通话·非方言 9,724 / China 总数 12,791**。
- [ ] **Tier 基线断言**：**(3,067, 2,303, 418, 346)**，即 Tier 1 = 2,303、Tier 2a = 418、Tier 2b = 346（证据补回）。
- [ ] **交叉校验**：重算 Is_Dialect 与 CSV 列逐行一致；全量方言片（含非 China）= 3,360；数据集总行数 = 63,025；空语言 China 电影 = 0（含 `EMPTY_LANG_DEFAULTED` 默认普通话）。
- [ ] **方案 A 不变量**：外语排首排除恰 56 部（`PLAN_A_EXCLUDED`）；Language_Code 不变量（code2 = Chinese 类且 Is_Dialect=0、code3 = 全量 Is_Dialect=1）。
- [ ] **Tier 2b 证据审查不变量（v4.1）**：主表补回证据恰 346 部且全部 Is_Dialect=1；`Dialect_Evidence == TIER2B_EXCLUDED` 恰 354 部且全部 Is_Dialect=0。2020–2026 新发现的 Tier 2b 使用 `LANG_BACKFILL_TIER2B_EXCLUDED`，不计入这 354。
- [ ] **审计名单生效**：审计排除 22 部与戏曲/演唱会排除 49 部不出现在任何 Is_Dialect=1 行。
- [ ] **测试全绿**：`python -m unittest discover -s tests -v`。
- [ ] **样本指纹**：`data/cleaned/sample_manifest.json` 指纹 = `883fe6f7fbe0fce781e5bbdccfd7a404e827c22b934324bca473248762f989c1`（v4.6 语言回填 + 《隐入尘烟》补收；回填前为 `51013700ec…`）。
- [ ] **评分统计口径**：各分组无评分影片数 = 0。

### 9.2 关键结论复核（当前基线数字）

| 指标 | 方言片（3,067） | 普通话/非方言片（9,724） |
| --- | --- | --- |
| 均分 | **6.62** | 6.11 |
| 烂片率（<5 分） | **6.5%** | 24.5% |
| 高分率（≥8 分） | 9.5% | 11.9% |

分层：Tier 1（2,303）均分 6.53 / 烂片率 6.6% / 高分率 6.9%；Tier 2a（418）6.67 / 8.9% / 13.4%；Tier 2b（346，证据补回）7.12 / 2.9% / 22.3%。核心结论不变：**方言片均分显著高于普通话片，烂片率约为其 1/3.8**（v4.3 剔除戏曲/演唱会类高分非叙事片后，方言组高分率下降 10.5%→9.5%，低于普通话组，结论更干净）。2010 分界点：2010 前方言落后普通话 0.32 分，2010 后反超 +0.88 分。

> 口径变更披露：对外核心数字（3,436 / 6.56 / 8.6% 等 v4.0 基线）自 v4.1 起整体替换为本表数字；方向性结论（方言 > 普通话）不变。

### 9.3 人工复核清单（单部影片）

1. 语言字段原文是否完整录入（分隔符切分无残片）？
2. 命中的白名单标签是否真实指向中国境内语言（排除 E5 国别歧义）？
3. 首个标签是否外语（方案 A 判定是否成立）？
4. 层级判定（有无普通话标签 + 首位标签）与决策树是否一致？
5. 少数民族语言影片：制片地区与说话人社区是否支持"中国境内使用"归属？
6. **（v4.1）Tier 2b 影片**：`Dialect_Evidence` 是否记录了补回证据？强制复核的 11 部（标杆/低置信）是否已完成记录卡？（✅ 已完成：用户 2026-08-15 复核确认无问题、维持补回；看片记录卡归档待办保留）
7. 存疑影片一律进 `data/review_queue.csv` 复核队列，不直接改数；核验使用轻量 Codebook 30 秒记录卡（`data/codebook_review.csv` 模板）。

---

## 10. 版本说明

### 10.1 口径声明

本文件的**判定规则**自 2026-08-15 起作为数据清洗、报告生成与前端叙事的统一依据（规则口径 v4.1）。**数据基线**随发布快照更新，现行为 v4.6（2026-08-30，豆瓣语言回填）；以 `scripts/freeze_constants.py` 与 `python scripts/verify_freeze_readiness.py` 为准。此前的定义文档继续保留为历史与方法论文献：

| 文档 | 角色 |
| --- | --- |
| 《方言电影定义_学术标准与操作判定.html》（v2.1） | 操作内核（判定规则原文） |
| 《方言电影定义_文献综述与操作标准_v3.html》 | 学术论证层（文献补全，不产生新判定规则） |
| 《方言电影轻量版_Codebook_v2.0.md》（v2.0.1） | 人工核验工具（三步判定法 + 记录卡） |
| 《方言电影定义_最终版_v4.0_Final.md》 | v4.0 定稿（方案 A / 审计修正固化，保留为历史文献） |
| `scripts/dialect_defs.py` | **唯一代码事实来源（SSOT）** |
| **本文件（规则 v4.1 · 数据 v4.6）** | **现行统一定义依据** |

### 10.2 相对 v2.1 / v3 / v4.0 的继承、修订与新增

| 类别 | 内容 |
| --- | --- |
| **继承（不变）** | v2.1 全部判定规则：白名单显式判定、无间接规则、外语/手语/戏曲排除、少数民族语言纳入、普通话归一化、Tier 决策树；v3 全部学术论证与占比推断口径；v4.0 全部成果（方案 A、审计排除名单、F1/F6 治理、交叉校验与烂片率分母防御断言、复核队列与抽样核验机制、冲突裁决规则） |
| **新增（v4.1）** | ① **Tier 2b 证据审查**（§5.5）：默认排除 + 证据漏斗（E1–E4 正向 / N1–N2 负向）+ 灰区逐部补判（LLM/离线路径）+ 标杆片与低置信案例强制人工复核；② 主表新增 `Dialect_Evidence` 列（补回证据编号/类型，排除者记 `TIER2B_EXCLUDED`）；③ `data_processor.py` 重建守卫（防重算撤销重分类）；④ 白名单与溯源文件（`tier2b_evidence.csv` / `tier2b_gray_zone_review.csv` / `tier2b_recovered.csv` / review_queue 425 条补判结论）；⑤ 回归测试 `Tier2bEvidenceReviewTests`（5 项） |
| **明确不改变** | 项目原始研究目标（中国方言电影的数据故事）、统计口径（Region=China）、Tier 1 / Tier 2a 判定（完全不受影响）、"占比为推断非实测"的诚实性声明 |

### 10.3 当前数据基线快照（2026-08-30 v4.6：豆瓣语言回填）

| 指标 | 数值 |
| --- | --- |
| 数据集总行数 | 63,025（含 delivery_20260817 合并 9,541 条） |
| 全量方言片（含非 China） | 3,360 |
| China 制片电影总数 | **12,791** |
| China 方言片 | **3,067**（占 China 24.0%） |
| └ Tier 1 / 2a / 2b | 2,303 / 418 / 346（证据补回；新 Tier 2b 默认排除另计） |
| China 普通话/非方言片 | **9,724**（占 China 76.0%） |
| 第一制片国（v4.5，2026-08-24） | 空格分隔制片国取首位；西德/东德归入 Europe；脚本 `apply_first_listed_region_20260824.py` |
| 豆瓣语言回填（v4.6，2026-08-30） | delivery 缺列按豆瓣「语言」回填；失败 China 默认普通话并戳 `EMPTY_LANG_DEFAULTED`；Wikidata P364 只补 `Language_Code` 空缺 |
| 《隐入尘烟》语言修正补收（2026-08-30） | delivery 缺列 + Wikidata 普通话误标；对照豆瓣现页改为甘肃方言/汉语普通话（甘肃方言排首）、Tier 2a |
| 阿嬷语言修正补收（v4.4，2026-08-19） | 《给阿嬷的情书》：缺语言列回填普通话后对照豆瓣实页改为潮汕话居首、Tier 2a |
| 戏曲/演唱会审计排除（v4.3） | 49 部；名单 `OPERA_CONCERT_EXCLUDE_MOVIE_IDS` |
| 空语言 China 电影 | 0 |
| 方案 A 排除 | 56 部（`PLAN_A_EXCLUDED`；v4.6 豆瓣回填后 +2） |
| Tier 2b 计人口径 | 346；`tier2b_recovered.csv` 为 286 行 LLM yes 子集，不是 348 部白名单文件 |
| 样本指纹（sample_manifest） | `883fe6f7fbe0fce781e5bbdccfd7a404e827c22b934324bca473248762f989c1`（完整值见 §9.1） |

> 基线演进链（可追溯）：… → **v4.4 阿嬷补收 (3,045, 2,289, 410, 346)** → **v4.5 第一制片国（方言计数不变；China 12,858→12,791，普通话 9,813→9,746，指纹 ecd258a80e…）** → **v4.6 豆瓣语言回填 (3,066, 2,303, 417, 346)** → **《隐入尘烟》补收 (3,067, 2,303, 418, 346)，指纹 883fe6f7fb…**。旧口径数字一律不再用于新报告。

---

## 附录：相关文件索引

| 文件 | 用途 |
| --- | --- |
| `scripts/dialect_defs.py` | 定义 SSOT（白名单、排除名单、判定函数） |
| `scripts/score_tier2b.py` | v4.1 证据漏斗规则引擎（含标杆影片清单与关键词表） |
| `scripts/llm_judge_tier2b.py` | v4.1 灰区逐部补判（LLM 在线 / 离线回填 --apply 双路径） |
| `scripts/apply_tier2b_reclassify_20260815.py` | v4.1 落地脚本（备份/重算/Dialect_Evidence/指纹同步） |
| `scripts/gen_report_strict.py` | 严格口径报告数据生成（含交叉校验断言与 Tier 2b 证据列感知） |
| `scripts/history/gen_html_report_static.py` | 完整版/方言片版 HTML 报告生成（历史脚本；现行明细报告用 `gen_dialect_report.py`） |
| `data/cleaned/derived_movies.csv` | 主数据表（Is_Dialect / Language_Code / Dialect_Evidence） |
| `data/cleaned/sample_manifest.json` | 纳入标准、阶段计数、SHA-256 指纹 |
| `data/archive/backups/` | 本地谱系备份已清理；历史状态用 `replay_v44_baseline.py --full-rebuild` 重建 |
| `data/tier2b_evidence.csv` | 现行证据漏斗得分（脚本读取路径） |
| `data/tier2b_gray_zone_review.csv` | 现行灰区补判记录 |
| `data/tier2b_recovered.csv` | **286 行 LLM 补判 yes 子集**；计人口径 346 见 freeze_constants |
| `data/archive/analysis/tier2b_*.csv` | 同名三件套的 v4.1 归档快照（与根级哈希不同，保留作溯源） |
| `data/dialect_films/report_data_strict.json` | 报告数据载荷（统计与明细） |
| `data/archive/analysis/plan_a_excluded.csv` 等 | 方案 A 排除/标注清单 |
| `scripts/freeze_constants.py` | 现行基线常量（China / Tier / 排除人数） |
| `scripts/verify_freeze_readiness.py` | 冻结前可执行验收（20 项） |
| 《方言电影数据清理与修改报告.html》 | 外语为主影片清理与第一语言检查建议原文 |
| 《方言电影项目_前期工作Review报告_20260815.md》 | F1–F10 代码级审查与版本基准结论 |
| 《方言电影轻量版_Codebook_v2.0.md》 | 人工核验三步判定法与记录卡 |
